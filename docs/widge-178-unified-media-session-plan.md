# WIDGE-178 unified media session implementation plan

This is the living execution plan for Plane item **WIDGE-178, Unify embedded media sessions and presentation ownership**.

For this item only, this file is implementation-owned after assignment. The platform implementation task must update it continuously. It is not a retrospective document and it must not be left stale until the end. Its purpose is to preserve exact decisions, completed work, failures, evidence, and remaining work across context compactions.

## Status ledger

| Field | Current value |
| --- | --- |
| Work item | WIDGE-178 — Unify embedded media sessions and presentation ownership |
| Lane | Platform; serialized SDK, Bridge, host, and bundled-widget integration |
| Production source baseline | `dfc43f6a8a98ca83f74940785edd4e937de2a746` on local `main` |
| Assignment baseline | `dfc43f6a8a98ca83f74940785edd4e937de2a746`; latest Plane dispatch comment supersedes the stale body baseline |
| Implementation owner | Standing platform implementation task |
| Branch | `codex/widge-178-unified-media-session` |
| Worktree | `C:\Users\dwive\.codex\visualizations\2026\08\11\019fef3b-7e94-70f0-b329-3551f8dd805b\widge178` |
| Current phase | `[~]` Fullscreen compatible-authority and retained Link-player correction has a green audited Release gate and is at its exact stage/commit checkpoint |
| Last completed checkpoint | `553e463b87d72a2544014d6096ea86daedd6c8e5` (`[WIDGE-178] commit compact media after pinned resize`), tree `1bcf29f282ed0cba07cb52e2b8e90c4023373eda`; exact scope: this plan plus `WidgetSurfaceCoordinator.cpp/.h` and `main.cpp`. The audited `Release -SkipTests` gate is green with only the four pre-existing `main.cpp` C4244 conversion warnings. |
| Next checkpoint | Exact-stage/diff-check the five-path source scope, create the local physical candidate commit, verify clean identity/artifact hashes, and return it for reviewer launch. |
| Last updated | 2026-09-03 by implementation agent after the pinned-resize candidate commit |

### Current pinned-resize candidate evidence

- **Source-review disposition:** pending reviewer review; this is a local, unintegrated candidate and no physical launch has been requested by this task.
- **Physical gate:** compact-pinned embedded media must stay continuously visible through both width/height growth and shrink; the resize record must show an exact-session committed viewport after the coalesced paint. The host must not park, retarget, recreate, or otherwise retire the session merely because the geometry is pending.
- **Deferred tests:** none ran under physical-first ordering. After physical acceptance, run only the directly owned pinned/embedded-media resize and authority-focused selectors.
- **Audited Release artifacts:** `OverlayHost.exe` `86330369FE5B5A0A40434B4FE71492397A0D128F82E7D528FA090B0ED8575A1C`; runtime `WidgetBridge.exe` `11FE9D6823D44FB61A3D8B952B7EDD50E934D1158DA26065AB06C9BA47804B32`; runtime `WidgetWorkerHost.exe` `39BAAA35D214499F48D7D3489472AED3C9A62169ECCD56CFCD0FD5E669693E4F`; `widget-catalog.json` `0FAF10999798939730B18AF1630B65B9993B8192649A6E1F58C023B37E915514`.

Progress marks used throughout this file:

- `[ ]` not started
- `[~]` in progress
- `[x]` completed with evidence recorded
- `[!]` blocked or stopped; record the exact red and owner
- `[-]` deliberately removed, superseded, or out of scope; record why

## Fullscreen follow-up checkpoint

- `[~]` Fullscreen source correction is in progress on top of clean checkpoint `e3a638acc3dc3d2d23282ca55cf5fbfd553e5cbc`. It adds a host-owned exact committed fullscreen checkpoint with durable session/document identity, runtime identity through the session key, presentation generation, snapshot sequence, and committed endpoint geometry. The checkpoint is staged from the fullscreen frame and published only after successful composition commit, HWND placement, and media reconciliation; repaint uses the same rule.
- `[~]` Generic reconciliation now treats only that exact checkpoint as fullscreen overlay layout. It must not use ordinary `committedWidgetVisualState_`, which fullscreen intentionally does not publish. The coordinator-only hide is suppressed only while the exact fullscreen request remains current, and transaction-owned `WM_SIZE` does not revoke the controller after fullscreen placement.
- `[!]` Separate physical red, out of this fullscreen correction: at 12:47:47.448 a new YouTube Play-a-link session faulted in `RichMediaSurfaceCoordinator::Initialize -> RecoverExitedSharedEnvironment` with `shared-environment-exited-with-live-controller`, HRESULT `0x800700AA` (`ERROR_BUSY`). Browser-process-exited was set while shared live controller owners remained nonzero. Existing WebView2 processes and the Windows Application log supplied no browser-exit cause. The shared generation cannot presently broadcast-retire its live sessions and recreate. This is the plan's existing unchecked shared-environment recovery risk; do not alter it in this fullscreen slice.
- `[ ]` Before build: review the exact `main.cpp` follow-up diff for session-key, generation, geometry, endpoint, and non-fullscreen regression boundaries. No new tests are authorized before physical fullscreen acceptance.

## Shared environment recovery follow-up

- `[~]` BrowserProcessExited is a failed exact shared WebView2 environment generation, not a per-controller renderer fault. The pinned WebView2 `ICoreWebView2BrowserProcessExitedEventArgs` interface exposes both browser PID and `BrowserProcessExitKind`; record those bounded diagnostics with generation, live-controller count, and waiter count only.
- `[~]` The host will post one generation-keyed recovery event, retire every controller/session bound to that failed generation exactly once, preserve other generations and widget workers/providers, and re-resolve the registry record after every `Initialize`/synchronous callback before claiming admission.
- `[~]` New admissions seeing a retired generation defer into one recovery waiter path rather than faulting `ERROR_BUSY`; after old owner drain, one current retained declaration begins a replacement shared environment and other current waiters join it. Removed/stale declarations are never resumed.
- `[ ]` Physical-first: no new or changed tests before the recovered-generation candidate is accepted. Do not modify the accepted fullscreen behavior or the separate unknown root cause of the original browser exit.

## Compaction recovery procedure

After every context compaction, restart, or handoff, do these steps before changing source:

1. Read this entire file, including the latest checkpoint, red/correction log, changed-file ledger, and remaining unchecked tasks.
2. Confirm the absolute worktree path, current branch, `git status --short`, `git log -5 --oneline --decorate`, and the diff from the assignment baseline.
3. Reconcile the repository state against this file. Source and test evidence win over an old checkbox; correct this file immediately if they disagree.
4. Resume only the current `[~]` phase or the first valid `[ ]` dependency after it. Do not repeat a completed gate merely because context was compacted.
5. Update the status ledger and checkpoint before doing more work.

The implementation task must update this document:

- after every bounded implementation slice;
- whenever the contract, file scope, removal list, or phase order changes;
- after every genuine red and after each attempted correction;
- before every build, test run, package operation, commit, terminal report, or stop;
- before asking the reviewer for a decision;
- at completion with exact commits, artifact identity, hashes, residual risks, and physical acceptance steps.

Do not erase completed evidence or failed attempts. Append concise corrections so another context can tell what happened and what remains.

## Locked product and architecture decisions

These decisions came from the user and are not open design questions for this implementation:

1. Use one in-process, multi-session media manager. Do not introduce a separate media service or process.
2. Preserve one shared WebView2 environment and browser process group. Each resident media session owns its own composition controller and document inside that shared environment. This does not promise a single Windows renderer process; WebView2 may maintain a process group.
3. Support multiple widgets playing independent media sessions concurrently. Retaining or presenting one session must not tear down another session. Keep the existing four-session residency bound. Admission of a fifth session remains an explicit failure; do not silently evict a playing session.
4. A session has exactly one presentation state at a time:
   - `Parked`
   - `OverlayViewport`
   - `OverlayFullscreen`
   - `CompactPinned`
5. `Parked` means the controller/document remains resident and may continue provider-authorized playback, but has no visual composition endpoint. It is not a hidden viewport.
6. Overlay viewport, overlay fullscreen, and compact pin are presentations of the same controller/document. They are not separate media owners or reconstructed players.
7. Fullscreen and compact-pinned subsystems may continue to own their chrome, geometry, input routing, and endpoint arbitration. They must not own or infer media-session lifetime.
8. A declared session with no current `UI.MediaViewport` retains an already-resident exact session as parked. It must not instantiate a new background controller. Omitting the session declaration closes an existing session. No `RetainSessionWhenHidden` Boolean is needed.
9. Durable session identity is separate from widget render snapshot sequence. Ordinary presentation updates and media telemetry must not recreate the controller, exit fullscreen, or revoke a valid session merely because the snapshot sequence advances.
10. Geometry has three meanings:
    - `Ready`: a current admitted viewport and final valid geometry may be presented;
    - `Pending`: the session remains valid but the renderer has not produced usable current geometry, so presentation is deferred or parked without teardown;
    - `Invalid`: malformed, mismatched, non-finite, out-of-contract, or revoked geometry/identity fails closed.
11. Use one transition planner/reducer and one side-effect executor for attach, detach, retarget, visibility, bounds, clip, composition commit, parking, and teardown. Do not retain overlapping projection, fullscreen, parking, and lifetime reconcilers.
12. Preserve existing security allowlists, sealed package-resource validation, browser-exit recovery, bounded resource ownership, command-terminal guarantees, input behavior, focus behavior, and accessibility semantics unless the new ownership boundary requires a direct equivalent.
13. Make a direct breaking contract update across the repository. Do not add a legacy adapter, dual protocol, compatibility shim, deprecated aliases, fallback parsing, or a translation layer for old declarations.
14. Do not build exhaustive transition-table tests, a property/model-test framework, or broad compatibility fixtures. Add only the focused tests named in this plan and the minimum compile-time updates required by the new public contract.
15. This is one cumulative implementation run. Internal phases are checkpoints, not reasons to stop or return the work early.

## Target public contract

The implementation task must validate exact naming against current source before editing, then record any mechanical naming correction here. The semantic contract is fixed:

Implementation naming checkpoint (2026-09-03): the preferred names are accurate in the current source and are final for this direct break: `EmbeddedMediaSession`, `WidgetView.EmbeddedMediaSession`, `MediaPresentationKind`, and `SupportedPresentations`. `UI.MediaViewport` will bind the declared session. No alias or alternate spelling is required.

- Replace `EmbeddedMediaSurface` with a session declaration whose name communicates lifetime, preferably `EmbeddedMediaSession`.
- Replace `WidgetView.EmbeddedMedia` with the corresponding session property, preferably `WidgetView.EmbeddedMediaSession`.
- Keep one optional declared media session per widget view. The host manager supports multiple simultaneous sessions across widget instances.
- Keep `UI.MediaViewport(...)` as the declarative, non-interactive visual placement for the current session. Its binding is the session ID, not a second lifetime owner.
- Replace the two alternative-presentation Booleans with one closed collection, preferably `IReadOnlyList<MediaPresentationKind> SupportedPresentations`, whose current values are:
  - `OverlayFullscreen`
  - `CompactPinned`
- Inline `OverlayViewport` presentation is requested by the presence of a matching `UI.MediaViewport`; it does not need a redundant capability value.
- Remove `CompactPinnedPresentation`, `RetainSessionWhenHidden`, and `OverlayFullscreenCapable` from protocol, SDK, Bridge DTOs, native declarations, validators, samples, tests, and documentation.
- A viewport requires a declared session whose ID matches exactly.
- A declared session may contain zero viewports; that means `Parked` and is valid without an extra flag.
- A view may contain at most one media viewport for its one declared session.
- Fullscreen and compact pin may be entered only when the declared capability exists and the host has current valid command/presentation authority.
- The session key must include admitted widget/catalog identity, widget instance, runtime generation, and declared session ID. Presentation generation and snapshot sequence must not be part of durable document identity. They remain current authority/correlation inputs and may advance without replacing the session.
- Resource/configuration identity changes that cannot safely apply in place close and replace the session deliberately. Telemetry, pending-command, accessible-label, and presentation-only changes must not be mistaken for resource identity changes.
- Durable sealed document/resource identity is limited to the entry asset, admitted resources, frame-origin/security policy, and exact durable session key. Surface sizing, aspect ratio, `SupportedPresentations`, seek step, and commands are mutable presentation/command authority and update in place; `SameEmbeddedMediaResourceContract` must not remain the session-recreation predicate because it currently mixes both categories.
- Increment the protocol version once for the direct contract. Update the source-of-truth constants/schema and regenerate checked-in native protocol output through the repository's canonical generator. Do not hand-edit generated output.
- Remove the SDK fullscreen-state callback/input-context plumbing if the Phase 0 inventory confirms there is still no production consumer: `Widget.OnOverlayFullscreenChangedAsync`, `ControllerInputContext.OverlayFullscreenPresentation`, `ControllerInputEvent.IsOverlayFullscreenActive`, and the corresponding Bridge/runtime notification. Fullscreen remains host-owned.

If existing names make the preferred names technically misleading, update this section with the chosen direct names and the reason before implementation. Do not retain the old public members for convenience.

## Host ownership model

The intended shape is one `MediaSessionManager` (or an equivalently direct name recorded here) with:

- a bounded registry keyed by durable session key;
- the shared WebView2 environment owner/reference;
- one per-session record containing the coordinator/controller/document, sealed resource identity, last valid declaration, playback state, command correlation state, current presentation state, and endpoint attachment;
- one reconciliation input that combines the admitted declaration, current widget/lifecycle visibility, renderer viewport observation, fullscreen request/state, compact-pin request/state, and endpoint availability;
- one pure or side-effect-free transition plan from current state plus input to target state and required effects;
- one executor that performs ordered effects and commits state only after successful completion;
- explicit rollback or fail-closed behavior for partial attach/detach failures;
- idempotent reconcile for repeated equivalent inputs;
- browser-process/controller-exit handling that retires only the affected session unless the shared environment itself failed;
- an explicit admission failure for a fifth resident session rather than implicit eviction;
- a typed parking reason that distinguishes host lifecycle parking from a declared route with no viewport;
- direct session-keyed callbacks so re-entrant work for session A can never mutate whichever session happens to own an endpoint later.

Endpoint contention must park the non-winning session rather than destroy it. Fullscreen is one overlay endpoint. Compact pin uses the existing pinned endpoint policy. Returning an endpoint to a parked session must reattach its exact controller without navigating or replaying the load command.

### Compact media pin and pinned-surface interaction

`CompactPinned` participates in the pinned-surface system without becoming a second session implementation:

- `MediaSessionManager` owns the media controller/document, session state, playback observations, command correlation, and the transition into or out of `CompactPinned`.
- `WidgetSurfaceCoordinator` and the pinned-surface host continue to own the pinned HWND, declared pinned layout/chrome, committed `MediaViewport` geometry, controller routing, accessibility projection, DPI/placement changes, and ordinary pinned-surface lifetime.
- A widget may expose ordinary `PinnedLayouts` and opt its media session into `CompactPinned` in the same snapshot. The pinned-surface selector presents the ordinary widget layouts plus one host-provided **Compact media** choice when the current media session supports it.
- Compact media is a distinct host-provided selector entry, not an implicit reinterpretation of whichever authored pinned layout is selected. The existing `compactMediaPresentation()` shortcut that turns every selected pinned layout into compact media when the capability is present must be removed while retaining the single pinned HWND and its existing placement, focus, opacity, persistence, and teardown owners.
- `CompactPinned` remains a distinct presentation kind because it attaches the live WebView controller and supplies host-native media chrome, shortcut semantics, and accessibility. It is not a distinct HWND, placement engine, focus-mode implementation, opacity system, persistence owner, or teardown lifecycle.
- A compact media declaration requests the pinned endpoint through the manager. The manager validates the exact session and capability, resolves the current committed pinned viewport from the pinned-surface owner, and attaches the same controller only after valid geometry exists.
- Pinning never clones, reloads, or navigates the media document. Unpinning moves the session to `OverlayViewport` when a current valid ordinary viewport owns the overlay endpoint; otherwise it moves to `Parked`.
- Switching the one pinned endpoint from Compact media to an ordinary declared pinned layout parks the media session and renders that ordinary layout through the existing coordinator. Switching back reattaches the exact controller/document and restores compact chrome without replaying load.
- Retiring or recreating the pinned HWND detaches that endpoint and parks the session. It does not close the session unless the widget/session declaration, runtime, resource identity, controller, shared environment, or host itself has become terminal.
- An ordinary non-media pinned surface continues to work. If pinned endpoint policy permits only one visual endpoint, admission/arbitration is explicit and the losing media session parks without affecting its playback lifetime. Do not silently replace unrelated pinned content.
- A compact-pinned session and a different overlay/fullscreen session may remain active simultaneously because they own distinct endpoints and distinct coordinators in the shared environment.
- Compact commands and chrome updates must be session-keyed. Telemetry or re-entrant callbacks from a parked/overlay/fullscreen session must never update the compact chrome belonging to another session.
- Preserve the host-owned compact controller contract: `X` toggles play/pause; `LT`/`RT` seek backward/forward by the session's declared step; `LB`/`RB` select previous/next only when declared; `B` exits compact interaction to click-through; and `View` returns to the tray. Route every held/repeated action through the exact `CompactPinned` session key.
- Held buttons/repeat state, focus, and accessibility authority retire when compact presentation ownership changes, while the media session itself remains resident.
- Tray unpin/repin, widget cycling, overlay hide/show, pinned-window retirement, widget removal, runtime replacement, and host shutdown must each have one explicit manager transition and one terminal/non-terminal classification.

### Existing compact-pinned presentation mechanics to preserve

WIDGE-178 replaces fragmented ownership; it must port the mature placement and anti-flicker mechanics already on `main`. Do not simplify these into a detach/recreate or show-then-position path:

- [ ] Preserve the pinned aspect-fit and centering calculation in `WidgetSurfaceCoordinator::Paint`: use the pinned client area minus the one-DIP border, fit the declared aspect ratio, and center on both axes. Keep the synthetic `host.compact-media.viewport` bounds and clip coherent.
- [ ] Publish pinned media geometry only after the pinned render completes successfully. Preserve the `committedMediaViewport_`, `mediaViewportGeometryDirty_`, `nextCommittedFrameGeneration_`, and `CurrentMediaViewport(...)` invariants behind the new per-session presentation record.
- [ ] Dirty/recompute pinned geometry after resource-contract replacement, pinned-layout replacement or cycling, window move/resize, DPI/graphics lifecycle changes, and unpin/teardown. Reconcile media only after the pinned coordinator reports a successfully committed viewport.
- [ ] Preserve exact geometry authority: live pinned surface, committed region, matching session ID, and selected pinned snapshot still declaring that exact session/presentation.
- [ ] Preserve endpoint coordinate spaces. Ordinary overlay media uses content-local coordinates and the overlay content transform; compact media uses pinned-HWND endpoint-local coordinates.
- [ ] Preserve relative clipping in `OverlayCompositionSurface::CommitExternalContentPresentation`: offset the external visual by host bounds, normalize clip coordinates against the visual origin, and clamp to visual width/height.
- [ ] Preserve controller-local WebView2 bounds `{0, 0, width, height}` in `RichMediaSurfaceCoordinator::UpdateGeometry`. Never apply the host offset to both the DirectComposition visual and the controller, which recreates the historical double-offset bug.
- [ ] Compute raster scale from the actual destination owner HWND. Do not reuse overlay DPI when the compact-pinned window may be on another monitor.
- [ ] Resolve and validate destination HWND, exact committed viewport, bounds, clip, dimensions, DPI, and authority before mutating the live controller. `Pending` remains hidden/parked; `Invalid` fails closed.
- [ ] Preserve live retargeting. `BeginPresentationTransfer` hides the controller and disables input; `CompletePresentationTransfer` changes root target/parent, applies local geometry and destination scale, commits presentation, and only then reveals/re-enables the controller. Do not create another controller and do not expose stale/default coordinates.
- [ ] Preserve successful transfer ordering: create destination endpoint; resolve geometry; create visual target; hide/disable input; attach root and parent; apply local bounds and destination DPI; commit destination; retire source; reveal only if the destination is still current.
- [ ] Preserve composition-first visibility. Commit external visual bounds, clip, and visibility before `SetVisible(true)`; if WebView visibility fails, roll the composition presentation back to hidden.
- [ ] Preserve equivalent-presentation deduplication so playback telemetry does not cause redundant DirectComposition commits or flicker.
- [ ] Preserve atomic endpoint retirement: removal/root changes require composition `Commit` and the existing completion wait. An uncertain removal, commit, or completion result invalidates/rebuilds the affected composition graph rather than trusting partially mutated fields.
- [ ] Preserve unpin ordering through `beforeWindowRetirement_`: transfer or park media while the pinned HWND and committed geometry still exist, then destroy the pinned window.
- [ ] Preserve the unattached parking target. When no visual endpoint is ready, retarget the same controller to the unattached visual, keep it hidden/non-interactive, and retire the former visible endpoint only after successful retarget.
- [~] Preserve pinned host-chrome layering above the video visual, with bounds synchronized to the same media region and visibility based on controller focus, scrub state, and playback. Setup/placement chrome is now retained and the compact video viewport excludes its chrome lane; final composition verification remains pending.
- [ ] Preserve compact progress, finite/bounded seek target calculation, scrub preview/commit/cancel, repaint, and accessibility publication without making playback telemetry a presentation-owner change.
- [ ] Treat composition-graph invalidation as affecting every endpoint on that graph and reconcile every affected session from manager state; do not assume only the currently selected media session was affected.

Historical commits that introduced or corrected these invariants must be inspected as evidence during Phase 0: `b2623de4` (committed pinned geometry/frame generation), `0958bb38` (visibility reconciliation), `8260e7fb` (presentation stability), `0195ece3` / `ac839ce3` / `ac58b39b` / `a54ba422` (compact presentation, ownership, composition, progress and seeking), and the integrated WIDGE-174 chain including atomic unpin, attached retarget, parking target, fail-closed endpoint recovery, geometry deferral, and refresh authority. Port the behavior into the new manager boundary; do not preserve the old mutable bound-session facade merely to reuse it.

## Removal inventory

The implementation task must identify exact symbols and update this list before deleting them. The following concepts are expected to be removed or collapsed:

- [ ] Old protocol and SDK presentation Booleans and their version checks.
- [ ] Hidden-retention validator special cases and error text based on `RetainSessionWhenHidden`.
- [ ] Host `EmbeddedMediaProjection` two-state ownership where it competes with fullscreen/parking.
- [ ] Parallel `EmbeddedMediaAuthority` fields that bind lifetime to exact snapshot sequence.
- [ ] Route-specific parking attach/detach helpers superseded by the manager.
- [ ] Fullscreen state that is reconstructed from each render snapshot rather than owned by the media session.
- [ ] Compact-pinned transfer code that moves lifetime ownership between subsystems.
- [ ] WIDGE-174/WIDGE-177 point-fix branches or conditions that become unreachable under the unified state model.
- [ ] Tests whose only purpose is old-Boolean compatibility or exhaustive combinations of obsolete declarations.
- [ ] Documentation that teaches old retention/presentation flags.
- [ ] SDK fullscreen-state notification/callback plumbing with no production consumer, if the Phase 0 inventory remains conclusive.
- [x] The embedded handoff-owner permutation harness in `main.cpp`, its `WRAIL_EMBEDDED_MEDIA_HANDOFF_TESTING` hooks, command-line/build selector, and claimed exhaustive case output.
- [ ] Media-specific source-text assertions that encode exact private method names or statement ordering rather than behavior.

Do not delete security validation, package-resource sealing, content allowlists, command correlation, input routing, accessibility semantics, diagnostics, or browser failure handling. Move them behind the new owner if necessary.

The rejected WIDGE-177 tail commit `849b0867` is evidence only. Do not cherry-pick it. Small ideas may be independently reimplemented only when they fit the unified manager and are recorded in the decision log.

## Phase 0 — Establish the exact baseline and inventory

- [x] Read `docs/implementation-agent-goal.md`, retrieve WIDGE-178 and its relations from Plane, and read this file in full. Plane has no `blocked_by`; WIDGE-178 starts after accepted WIDGE-54 and starts before WIDGE-175.
- [x] Create a new clean isolated worktree and branch from exact dispatch baseline `dfc43f6a8a98ca83f74940785edd4e937de2a746`; do not reuse the WIDGE-177 worktree.
- [x] Record branch, worktree, baseline commit, `git status`, and initial checkpoint in the status ledger.
- [x] Confirm the shared WebView2 environment creation path and current resident-session bound. `OverlayApp` owns one `RichMediaEnvironmentHandle` from `RichMediaSurfaceCoordinator::CreateSharedEnvironment()` and `kMaximumResidentMediaSessions` is exactly 4.
- [x] Inventory every direct declaration and consumer across `WidgetProtocol`, `WidgetSdk`, `WidgetBridge`, `OverlayHost`, generator/template inputs, YouTube, Embedded Media, CLI preview, tests, and directly affected docs. Repository-wide `EmbeddedMedia` plus old-symbol searches were captured before edits.
- [x] Record the exact planned file list in the changed-file ledger before editing.
- [x] Record which integrated WIDGE-177 prefix pieces the new design keeps, rewrites, or deletes. Keep its stale-geometry rejection, committed pinned geometry/frame generation, unattached parking target, transfer ordering, and route/lifecycle evidence; replace its exact-sequence lifetime branches and the rejected `849b0867` lifecycle-parking idea with manager-owned durable session identity.
- [x] Confirm that current `main` has one shared environment and up to four resident coordinators but still routes operations through the mutable `richMediaSurface_` / `embeddedMediaAuthority_` / `boundEmbeddedMediaSessionKey_` facade. No deviation from the reviewed baseline was found.

## Phase 1 — Replace the public declaration directly

- [x] Introduce the direct media-session declaration and closed presentation vocabulary in the managed protocol/SDK source of truth.
- [x] Replace the `WidgetView` property and `UI.MediaViewport` binding contract directly.
- [~] Remove all three old Boolean properties; no production parser/DTO/SDK consumer retains them, while templates/docs and focused source oracles remain to be updated.
- [x] Update semantic validation for zero-or-one viewport, exact session binding, and required endpoint capability.
- [x] Update protocol version requirements once at v42.
- [~] Update Bridge wire DTOs, resolver/materializer, declaration equality/resource identity, diagnostics, and validation; production wire is session-shaped and focused fixtures are being reconciled.
- [x] Run the canonical protocol generator and record its command and output paths. Generated `src/OverlayHost/WidgetProtocolPresentationContract.generated.h` from managed constants with the audited/network-enabled Release invocation.
- [ ] Update only the focused SDK/protocol tests necessary to prove serialization, validation, and direct removal of the old shape.
- [x] Remove obsolete fullscreen-state SDK/runtime/Bridge/host callbacks after the no-production-consumer inventory was confirmed.
- [ ] Update this file with the exact contract names and Phase 1 evidence before starting Phase 2.

## Phase 2 — Introduce the unified host manager

- [~] Add the bounded `MediaSessionManager` registry and per-session state owner. The provisional four-entry map exists, but its mutable `activeKey_`/selection facade was rejected in review and must be removed before integration; every operation will require an exact immutable key or exact endpoint-owner query.
- [~] Reuse the one shared environment; create one coordinator/controller/document per resident session. Per-entry coordinator creation exists; host integration is blocked on removal of the provisional selected-session facade.
- [x] Define the closed presentation state and explicit geometry readiness state. `PresentationState`, `GeometryState`, and typed `ParkingReason` are declared in the manager.
- [~] Add the transition planner and single side-effect executor. Invalid geometry faults through ordered teardown; endpoint arbitration parks the displaced exact-key owner; same-presentation geometry/owner/DPI/frame changes produce an explicit `Update` effect and commit only after success. Host effect migration remains in progress.
- [x] Separate typed sealed document identity from presentation sequence, mutable declaration/commands, and endpoint geometry.
- [x] Make equivalent manager reconciliation idempotent by comparing requested presentation plus exact committed endpoint geometry; telemetry with unchanged endpoint state plans `None`.
- [ ] Preserve per-session controller/document across Parked, OverlayViewport, OverlayFullscreen, and CompactPinned transitions.
- [ ] Keep `RichMediaSurfaceCoordinator` as the per-document WebView2 transport, sandbox, adapter-message, retarget, teardown, and shared-environment client. Remove its dependence on a mutable globally bound session rather than rewriting its proven security boundary.
- [x] Make every coordinator callback carry the immutable manager session key captured at creation; playback, invalidation, visibility, transition, compact/fullscreen input and held repeat relookup that exact key and reject an absent/replaced entry without rebinding another entry.
- [ ] Preserve bounded error mapping and diagnostics with session locator, source state, target state, and failing effect at debug severity where appropriate. Do not add general payload/value logging.
- [ ] Update this file with added files, responsibilities, and checkpoint evidence before removing old paths.

## Phase 3 — Remove competing host ownership

- [ ] Route ordinary overlay reconciliation through the manager.
- [ ] Route hidden/cycled widget lifecycle changes to `Parked` without destroying a declared session.
- [ ] Route fullscreen entry, geometry updates, input, B exit, accessibility, and teardown through the same session.
- [ ] Route compact pin, tray unpin/repin, pinned geometry, commands, and accessibility through the same session.
- [ ] Reuse the existing pinned-surface selection, HWND, placement, focus/click-through, opacity, persistence, and teardown machinery for Compact media; expose it as a host-provided choice alongside a widget's ordinary `PinnedLayouts` rather than a parallel pin implementation.
- [ ] Preserve the rule that ordinary pinned projections are declarative and do not smuggle in a second media viewport. Compact media is the explicit session-backed media choice.
- [ ] Ensure endpoint loss parks the session unless the declaration was removed or authority became invalid.
- [ ] Remove superseded projection, parking, exact-snapshot lifetime, and transfer ownership code.
- [x] Remove the mutable bound-session facade, including `SaveBoundEmbeddedMediaSession`, `BindEmbeddedMediaSession`, `CreateAndBindEmbeddedMediaSession`, `EnsureBoundEmbeddedMediaParkingTarget`, and their equivalent state fields; repository search finds none of the old globals/helpers or projection enum in `main.cpp`.
- [ ] Stop fullscreen rendering from overwriting ordinary `lastWidgetRenderResult_` or `committedWidgetVisualState_`; publish typed fullscreen geometry to the manager after the relevant composition frame commits.
- [ ] Prove ordinary playback telemetry does not exit fullscreen or replace the document.
- [ ] Prove a stale action/command still fails closed without making snapshot sequence the lifetime key.
- [ ] Update the removal inventory and record deleted symbols/files before Phase 4.

## Phase 4 — Update all in-repository widget consumers

- [ ] Update YouTube to declare one session on Player, Search, Settings, and other routes whenever the media session should remain resident. Player includes a viewport; non-player routes omit it and therefore park.
- [ ] Ensure YouTube no longer synthesizes route-specific retained surfaces or toggles presentation Booleans.
- [ ] Preserve YouTube playback, settings, fullscreen, captions route, commands, LT/RT seeking, and compact media pin behavior from accepted WIDGE-54.
- [ ] Update the Embedded Media sample to teach the direct session/capability contract.
- [ ] Update every other in-repository compile-time consumer found in Phase 0. Remove obsolete examples rather than wrapping old declarations.
- [ ] Update directly affected authoring and protocol documentation, including `docs/declarative-ui.md`, `docs/embedded-media-widget.md`, and `docs/adding-declarative-ui-elements.md` where their instructions touch the changed declaration or generator flow.
- [ ] Update package manifests/versions only where the repository's package contract requires it; record exact version changes.

## Phase 5 — Focused verification only

Do not create an exhaustive state-transition matrix. Do not create a property/model test framework. Do not duplicate equivalent test cases across layers. Prefer one authoritative test at the narrowest layer plus the minimum end-to-end/package proof.

- [ ] Managed protocol/SDK: one round-trip for the new session/capability shape and focused invalid cases for mismatched viewport, missing capability, and removed declaration.
- [ ] Native parsing/admission: one production-shaped valid snapshot and focused fail-closed coverage for invalid identity/geometry.
- [ ] Manager: a small set of scenario tests proving:
  - fullscreen remains active across command-free telemetry/snapshot advancement;
  - Player to Search or Settings parks and Player return reuses the exact controller/document;
  - widget cycling parks and restores the exact controller/document;
  - compact pin, unpin, and repin work from both visible and parked routes;
  - a widget exposing both ordinary pinned layouts and Compact media can switch between them on the one pinned endpoint while retaining the exact media controller;
  - two widget instances retain independent resident sessions concurrently;
  - pending geometry defers/parks while invalid geometry fails closed;
  - dropping the session declaration closes it.
- [ ] YouTube: focused package/widget checks for the new declarations on Player/Search/Settings and preservation of accepted settings/transport behavior.
- [ ] Embedded Media: focused package/sample validation against the new public contract.
- [ ] Real WebView smoke: create two coordinators from the shared environment, keep distinct controller/document authorities, park one, and verify independent playback observations without teardown.
- [ ] Run each focused gate once after the implementation is coherent. Record command, result, duration, and exact failing case for any red.
- [ ] Follow the same-issue second-red rule from `docs/implementation-agent-goal.md`; do not stop at the first correctable red.

## Phase 6 — Coherent Release/package candidate

- [ ] Update this file before starting the build and record the exact planned command.
- [ ] Run one coherent canonical Release/package build covering the host, Bridge/SDK, and affected bundled widget packages.
- [ ] Confirm a compiler actually ran; a setup-only or skipped result is not validation.
- [ ] Record all produced artifact paths, versions, sizes, and SHA-256 hashes.
- [ ] Confirm the worktree contains only WIDGE-178 scope plus this living plan.
- [ ] Run `git diff --check` and the repository's required package validation once.
- [ ] Do not install, launch, integrate, push, or mutate Plane from the implementation task.

## Phase 7 — Final implementation handoff

- [ ] Reconcile every checkbox, removal, changed path, red, gate, artifact, and residual risk in this file.
- [ ] Make one coherent local implementation commit or a minimal reviewable commit chain when a single commit would hide an important generated boundary. Record exact commit IDs and parent.
- [ ] Confirm branch/worktree cleanliness after the commit.
- [ ] Report exact baseline, commits, changed paths, removed legacy paths, tests, build/package artifacts, hashes, and manual acceptance checklist to the reviewer.
- [ ] Leave `gate:manual` in place. The reviewer owns integration, installation, launch, and user acceptance.

## Changed-file ledger

Populate before editing and keep current. Add one row per file or tightly coupled generated family.

| Path | Planned responsibility | Status | Evidence/notes |
| --- | --- | --- | --- |
| `docs/widge-178-unified-media-session-plan.md` | Living execution and recovery record | `[~]` | Reviewer-authored initial plan; implementation-owned after assignment |
| `src/WidgetProtocol/ViewModels.cs`; `ViewSnapshotValidator.cs`; `ProtocolConstants.cs`; `ProtocolVersionRequirements.cs`; `SnapshotJson.cs` | Direct session declaration, closed presentation vocabulary, validation, version admission, serialization | `[~]` | `EmbeddedMediaSession`, `SupportedPresentations`, parked zero-viewport semantics, and v42 source-of-truth edits present; final validation/parity pending |
| `src/WidgetSdk/Widget.cs`; `Elements.cs`; `UI.cs`; `WidgetPresentationDiff.cs`; `PublicApi.txt`; `README.md` | Public SDK shape, viewport binding, removal of unused fullscreen callback/input context, API/docs | `[~]` | Property/type/viewport binding and callback/input removal underway; PublicApi/docs pending |
| `src/WidgetBridge/BridgeProtocol.cs`; `EmbeddedMediaAssetResolver.cs`; `BridgeClientRegistry.cs`; `WidgetBridgeServer.cs` | Session-shaped wire/materialization/resource identity and removal of fullscreen notification | `[~]` | Managed bundle/resource authority and obsolete notification checks updated; exact wire/native parity pending |
| `src/WidgetRuntime/RuntimeProtocol.cs`; `WidgetProcessClient.cs`; `WidgetWorkerServer.cs` | Remove obsolete fullscreen notification plumbing and carry direct contract | `[~]` | Worker validation path removed; remaining transport inventory pending |
| `src/OverlayHost/WidgetBridgeClient.h`; `WidgetBridgeClient.cpp`; generated `WidgetProtocolPresentationContract.generated.h`; `EmbeddedMediaResourceContract.h` | Native parse/admission, generated parity, resource identity | `[~]` | Native declaration/property mechanical rename started; parser/capability semantics and generated parity pending |
| `src/OverlayHost/MediaSessionManager.h`; `MediaSessionManager.cpp` | Bounded four-entry durable registry, closed presentation/geometry state, transition plan/execution state | `[~]` | Exact-key registry, four-entry admission, shared-environment coordinator creation, one presentation state, endpoint-owner arbitration, fail-closed fault plan, typed committed endpoint geometry, Present/Update dedupe, immutable-key callbacks, and guarded post-terminal erasure exist; host effects/integration pending. No active key, widget-first selection, no-key accessor, detached fallback, or public raw clear remains |
| `src/OverlayHost/main.cpp` | Route overlay/fullscreen/compact/park/lifecycle/input/accessibility through manager; remove bound facade and exhaustive handoff hooks | `[~]` | Obsolete exhaustive handoff body/selector removed; direct manager access migration is in progress and old bound registry/facade must still be removed |
| `src/OverlayHost/RichMediaSurfaceCoordinator.h`; `RichMediaSurfaceCoordinator.cpp` | Per-session immutable callback identity and retained transport/retarget/security boundary | `[ ]` | Shared environment retained |
| `src/OverlayHost/OverlayCompositionSurface.h`; `OverlayCompositionSurface.cpp`; `WidgetSurfaceCoordinator.h`; `WidgetSurfaceCoordinator.cpp` | Existing endpoint/committed compact geometry integration, remove obsolete handoff-only hooks | `[ ]` | No parallel pinned HWND |
| `src/OverlayHost/build.ps1`; `OverlayHost.vcxproj`; `CMakeLists.txt` | Compile/register manager and remove obsolete handoff selector wiring | `[~]` | Obsolete `EmbeddedMediaHandoffTestsOnly` parameter/definition/execution removed; manager source/header are registered in all three host build owners. Add one target-local `RichMediaContractTestsOnly` switch that selects the executable's established `--contract-only` route, avoiding unrelated live-environment groups |
| `samples/YouTubeWidget/YouTubeVideoWidget.cs`; `YouTubeVideoWidget.Search.cs`; manifest/package-version/current-version tests as required | Declare one session across retained routes and preserve WIDGE-54 behavior | `[~]` | Player and parked-route declarations converted; package/test reconciliation pending |
| `samples/EmbeddedMediaWidget/EmbeddedMediaSampleWidget.cs`; manifest/package-version files as required | Provider-neutral direct session sample | `[~]` | Direct session and closed presentation list authored; package/test reconciliation pending |
| `templates/ControllerWidget/profiles/embedded-media.cs.template`; `tests/EmbeddedMediaScenarioTests.cs.template`; `README.embedded-media.md.template`; `template.json` | Direct session authoring template and validation | `[ ]` | Remove obsolete Boolean/permutation examples |
| `docs/declarative-ui.md`; `embedded-media-widget.md`; `widget-quickstart.md`; `widget-authoring-guide.md`; `adding-declarative-ui-elements.md` | Direct public contract, lifecycle, compact selection, and element-change guidance | `[ ]` | `docs/implementation-status.md` and every other reviewer-owned status/planning/history document are explicitly excluded |
| `tests/WidgetSdk.Tests/EmbeddedMediaSurfaceTests.cs`; `ProtocolVersionRequirementsTests.cs`; `Program.cs` | Focused managed session serialization/validation/direct-removal coverage | `[~]` | Existing SDK tests mechanically migrated to the direct names; semantic assertions and suite registration still require reconciliation |
| `tests/WidgetBridge.Tests/BridgeClientRegistryScenarios.cs`; `Program.cs` | Focused Bridge session/resource/command authority coverage | `[~]` | Existing Bridge fixtures mechanically migrated; behavior assertions remain to be reconciled without source-text ordering checks |
| `tests/YouTubeWidget.Tests/YouTubeWidgetTests.cs`; related exact package/current-version tests | Player/Search/Settings declarations and WIDGE-54 preservation | `[~]` | Existing YouTube assertions mechanically migrated; package/declaration semantics remain to be reconciled |
| `tests/WrailCli.Tests/ScenarioPreviewTests.cs`; template/CLI exact consumers | Compile/preview the direct contract | `[~]` | Existing CLI preview fixture mechanically migrated; templates and exact assertions remain pending |
| `src/OverlayHost/MediaSessionManagerTests.cpp`; directly affected `RichMediaSurfaceCoordinatorTests.cpp`, `WidgetBridgeCatalogTests.cpp`, `WidgetSurfaceCoordinatorTests.cpp`, `PinnedSurfaceHostTests.cpp`, `DeclarativeRendererTests.cpp` | Focused manager, shared-environment, parse, geometry, compact/ordinary selection and lifecycle proof | `[~]` | Existing native declaration consumers were mechanically renamed in five current test owners; manager tests and semantic cleanup remain pending |

## Removed legacy-path ledger

| Symbol/path | Replacement | Status | Evidence/notes |
| --- | --- | --- | --- |
| `CompactPinnedPresentation` | Closed supported-presentations collection plus manager presentation state | `[~]` | Removed from managed production contract/consumers; native/tests/docs pending |
| `RetainSessionWhenHidden` | Declared-session-without-viewport means Parked | `[~]` | Removed from managed production contract/consumers; native/tests/docs pending |
| `OverlayFullscreenCapable` | Closed supported-presentations collection plus manager presentation state | `[~]` | Removed from managed production contract/consumers; native/tests/docs pending |
| Host projection/parking/fullscreen lifetime overlap | One manager/reducer/executor | `[~]` | Obsolete in-host 63-case handoff permutation body and executable selector removed; production bound façade remains until manager extraction |
| Mutable bound-session facade | Direct immutable session-keyed manager operations and callbacks | `[x]` | Removed old global coordinator/authority/bounds/clip/bound-key/resident-map fields and all bind/save/transfer/suspend wrapper call sites from `main.cpp`; no wrapper compatibility layer remains |
| Embedded handoff-owner permutation harness | Focused manager scenarios plus one real shared-environment smoke case | `[x]` | `RunEmbeddedMediaHandoffOwnerTests`, CLI argument, macro definition, build selector, guarded coordinator/composition failure injection, and session-seeding hooks removed; repository search finds no remaining macro or harness symbol |

## Gate ledger

Do not record a gate as green unless the named command actually ran and produced its expected artifact or test count.

| Gate | Command | Result | Evidence |
| --- | --- | --- | --- |
| Baseline status | `git rev-parse HEAD`; `git branch --show-current`; `git status --short`; `git log -5 --oneline --decorate`; tracked-file count | `[x]` | HEAD `dfc43f6a8a98ca83f74940785edd4e937de2a746`; branch `codex/widge-178-unified-media-session`; initially clean; 951 tracked files |
| Managed protocol/SDK focused tests | Serialized build: `dotnet build tests/WidgetSdk.Tests/WidgetSdk.Tests.csproj --configuration Release --no-restore --nologo -- /m:1 /p:BuildInParallel=false`; selector: `dotnet run --project tests\WidgetSdk.Tests\WidgetSdk.Tests.csproj --configuration Release --no-build --no-restore -- --test-prefix "Embedded media session"` | `[x]` | Initial behavior run failed 0/1 because state-callback-only provider fixtures leaked into the HTML-media profile. After the bounded runner correction, serialized rebuild was green (0 warnings/errors, 22.27 seconds) and the exact replacement selector passed 1/1 in 1.02 seconds |
| Protocol contract generation | `dotnet run --project tests/WidgetProtocol.ContractParity.Tests/WidgetProtocol.ContractParity.Tests.csproj -c Release -- --write` with supported unrestricted network access and NuGet audit enabled | `[x]` | Green; canonical generator wrote `src/OverlayHost/WidgetProtocolPresentationContract.generated.h` from `ProtocolConstants` |
| Native parser/manager focused tests | `pwsh -NoProfile -File src\OverlayHost\build.ps1 -Configuration Release -RichMediaContractTestsOnly`; `pwsh -NoProfile -File src\OverlayHost\build.ps1 -Configuration Release -WidgetBridgeCatalogTestsOnly` | `[x]` | Manager/fullscreen passed 48. After the recorded compile/admission/locator corrections and reviewer-authorized post-second-red resumption, native embedded-media boundary cases passed=13 and WidgetBridgeCatalogTests passed |
| Managed Bridge media authority | Exact `WidgetBridge.Tests` prefixes: `Embedded media resolution requires exact current publication authority`; then `Two provider-neutral media adapters resolve through one sealed contract`; then `Built embedded media sample completes the typed playback loop` | `[x]` | All three exact selectors green 1/1 after their recorded bounded fixture/oracle corrections |
| YouTube focused tests/package validation | One filtered `YouTubeWidget.Tests` invocation for `SnapshotUsesV26BoundedYouTubeAuthorityAndAccessibleNativeControls`, `RetainedVideoLinkRouteAndFullscreenStatesRemainValid`, `SearchRetainsOnlyAnEstablishedSessionAndPlayerReturnDoesNotReload`, `PlayerSettingsPublishBoundedPreferencesAndRetainLastObservationOnRejection`, and `ManifestIsCredentialFreeSuspendedAndImmutableVersioned`; minimum 5 | `[x]` | Direct already-built apphost green 5/5 after one source-proven Link capability correction; manifest v0.3.14 included |
| Embedded Media focused tests/package validation | SDK session selector plus managed Bridge built-sample selector | `[x]` | SDK `Embedded media session` green 1/1; exact built sample typed playback loop green 1/1 |
| Canonical Release/package build | `pwsh -NoProfile -File src\OverlayHost\build.ps1 -Configuration Release -SkipTests` with supported unrestricted network access and NuGet audit enabled | `[x]` | Green replacement after the two independently classified first-red corrections: native compilation completed with only the existing numeric-conversion warnings; WidgetBridge and WidgetWorkerHost published; bundled packages sealed; built `src/OverlayHost/out/Release/OverlayHost.exe` |
| Reviewer post-fix native suites | `build.ps1 -Configuration Release` with `-RichMediaContractTestsOnly`, `-PinnedSurfaceTestsOnly`, `-WidgetSurfaceTestsOnly`, `-WidgetBridgeCatalogTestsOnly`, then `-SkipTests` | `[x]` | Contract 48 (two new regression cases included); PinnedSurface 449; WidgetSurface 331; native embedded-media boundary 13 plus catalog; coherent Release green with only the pre-existing C4244 numeric-conversion warnings |
| `git diff --check` | To be run once on final range | `[ ]` |  |
| Physical acceptance | Reviewer/user after installation | `[ ]` | Manual gate remains open |

## Red and correction log

Append; do not overwrite prior entries.

| Attempt | Phase/gate | Exact red | Classification | Correction/result |
| --- | --- | --- | --- | --- |
| 0 | Planning | None | N/A | Implementation not started |
| 1 | Coherent Release `-SkipTests` native compile | `main.cpp(5661)` undeclared `mediaKeys`; `(8021)` stale 17-argument `SendControllerInput`; `(12597)` redefined `key` causing `(12604)` uninitialized/wrong-type cascade | First genuine red for one evidence-proven cause: incomplete mechanical `main.cpp` session-vocabulary/manager migration | Bounded correction applied: capture `mediaSessions_.Keys()` once for loop/diagnostic, align the select-popup call with the current 16-argument signature, and use one unambiguous `sessionKey` throughout both held-media authority paths. The exact replacement Release is next; a repeat of this cause is the mandatory second-red stop |
| 2 | Replacement coherent Release `-SkipTests` managed Bridge publication | Native compile cleared attempt 1; `WidgetBridgeServer.cs(460,36)` failed CS0117 because the reply still names removed `BridgeMessageTypes.EmbeddedMedia` while the direct-break constant is `EmbeddedMediaSession` with the unchanged wire value `embedded-media` | First genuine red for a distinct Bridge reply transport-vocabulary cause | One-line correction to `EmbeddedMediaSession`; replacement coherent Release green through native compile, managed publication, runtime/package materialization, and final `OverlayHost.exe` |
| 3 | Focused WidgetSdk `Embedded media session` selector | 0/1; `KeyNotFoundException: The given key was not present in the dictionary` | First genuine red for a distinct test-owned direct-contract locator cause. Source proves `EmbeddedMediaAdapterConformanceGate` still requests removed JSON property `embeddedMedia`, while protocol/SDK/native use `embeddedMediaSession` | Change only that exact conformance locator to `embeddedMediaSession`, then run the same focused selector once; a repeat of this cause is the mandatory second-red stop |
| 4 | Replacement focused WidgetSdk selector with no-restore compilation | Exit 1 before test execution; sole output: `The build failed. Fix the build errors and run again.` No compiler diagnostic and no affected test/conformance artifact timestamp changed | First red for a distinct reported pre-test build issue. The corrected behavior red was `KeyNotFoundException`; gate identity alone does not prove a shared cause | User clarified the stop rule applies to the second genuine red from the same issue/error. Run one bounded explicit-verbosity no-restore diagnostic build to classify this distinct failure; do not repeat the unchanged selector or make an unproven correction |
| 5 | Bounded no-restore diagnostic build for the distinct pre-test issue | `dotnet build tests\WidgetSdk.Tests\WidgetSdk.Tests.csproj --configuration Release --no-restore --verbosity normal --nologo` exited 1 in 1.87 seconds with `Build FAILED`, `0 Warning(s)`, `0 Error(s)`, and no compiler diagnostic | Second genuine red for the same silent WidgetSdk pre-test build issue seen in attempt 4 | Mandatory stop under the corrected rule. No source correction, selector rerun, or later gate attempted; reviewer disposition is required to authorize a deeper build-system diagnostic |
| 6 | Reviewer-authorized diagnostic-only MSBuild trace | Release/no-restore build with binary and diagnostic file log exited 1 in 1.90 seconds. `_GetProjectReferenceTargetFrameworkProperties` task 21 invoked `MSBuild` over six references with `BuildInParallel=True`; only `WidgetSdk.csproj` started, completed `GetTargetFrameworks`, and returned valid `net8.0` metadata. `PlatformBroker`, `EmbeddedMediaAdapterConformance`, `ClockWidget`, `EmbeddedMediaWidget`, and transitive `WidgetProtocol` never started; the parent task then returned false with 0 warnings/errors | Build-engine/project-reference fan-out failure before compilation. Normal SDK resolution completed; no `CoreCompile`, analyzer, source, test, restore, or cache target produced the red. No Application Error/.NET Runtime crash event was recorded in the same minute | Diagnostic artifacts: `artifacts/widge178/WidgetSdk.Tests.build.binlog` (338023 bytes, SHA-256 `317C53296A82F6FCE16C99620684336BED3EA8574C5EE78E846A23E93E0373CE`) and `WidgetSdk.Tests.build.diag.log` (752434 bytes, SHA-256 `AA6C5F768B423A6FE6C79D647B41CD7B9FB057FE73025E75CB1A957693AC428B`). Proposed bounded invocation correction: one `--no-restore` build with `/m:1` and `BuildInParallel=false`; no product/test edit. Stop for authorization before applying it |
| 7 | Reviewer-authorized serialized no-restore SDK build | `dotnet build ... --no-restore -- /m:1 /p:BuildInParallel=false` compiled every referenced project and `WidgetSdk.Tests`; 0 warnings, 0 errors, 22.08 seconds | Green infrastructure correction; the parallel fan-out issue did not recur under the exact serialized disposition | Proceed once to the corrected focused selector; this does not erase attempts 4-6 or reclassify their evidence |
| 8 | Corrected focused WidgetSdk `Embedded media session` selector | 0/1; adapter conformance failed at `terminal-playback-state: rate:cued-late-success expected ready; got paused` | First genuine red for a distinct test-runner profile-leak cause. The generic requirements loop already exercises HTML-media `SetPlaybackRate`; the subsequent helper mutates only the state-callback fake (`player.state`, available rates, callback suppression/timeouts) while the HTML adapter observes `FakeMedia`, so `player.state = 5` cannot produce HTML `ready`. The same helper's later availability/timeout cases are likewise state-callback-specific | Gate only the three provider-specific helper families on `profile === 'state-callback'`, preserving the generic command exercise for both profiles. Rebuild the affected managed artifact serially and run the exact selector once; a repeat of this cause is the mandatory second-red stop |
| 9 | Exact replacement focused WidgetSdk `Embedded media session` selector | Green: `PASS Embedded media session and native viewport are bounded protocol v42`; `1/1 tests passed` | The bounded profile-owner correction resolved attempt 8; no second genuine red occurred for this cause | Continue to the narrow native manager/fullscreen owner gate |
| 10 | Native `RichMediaContractTestsOnly` compile | `RichMediaSurfaceCoordinatorTests.cpp(472)` C2653/C3861 for unqualified coordinator name; `OverlayCompositionSurface.cpp(271,332)` C2039 because `IDCompositionVisual2` has no `SetOpacity`; build exited 2 before execution | First genuine red for the newly exercised native contract translation set. One test namespace defect and one composition-interface ownership defect are independently source-identifiable within the same failed compile invocation | Trace the established DirectComposition opacity owner/interface, qualify the test call, make one bounded correction, then run the exact replacement selector once. Same-cause second red stops |
| 11 | Exact replacement native `RichMediaContractTestsOnly` | Green after compiling the focused native translation set; `RichMediaSurfaceCoordinator contract cases passed=48` | The source-proven namespace and `IDCompositionVisual3` opacity-owner correction resolved attempt 10; no second genuine red occurred | Continue to the narrow native protocol/parser admission gate |
| 12 | Native `WidgetBridgeCatalogTestsOnly` compile | `WidgetBridgeClient.cpp(4836)` C2660: `BuildControllerInputRequestEnvelope` does not take 18 arguments; current declaration is at line 4187 | First genuine red for a distinct stale focused-test helper call after direct contract migration; no test executed | Compare signature/call, remove only the obsolete argument, and run the exact replacement once. Same-cause second red stops |
| 13 | Replacement native `WidgetBridgeCatalogTestsOnly` | Compilation cleared attempt 12, then execution failed: `ordinary/recovery checkpoint admission rejected the affected production-shaped MediaViewport node defaults` | Distinct first genuine behavioral red; the earlier stale-call compile cause is resolved | Trace exact production parser/default admission and use at most one bounded owner-correct correction plus exact replacement |
| 14 | Replacement native `WidgetBridgeCatalogTestsOnly` after v42 fixture correction | Prior admission predicate cleared; execution advanced and failed `embedded media mutation source was absent` | Distinct first genuine test-construction red in an exact string mutation locator; not the v42 admission predicate | Identify the exact stale locator and use at most one bounded mechanical correction plus exact replacement |
| 15 | Bounded read-only mutation-locator audit | Reconstructed the function's four raw JSON fixtures and tested every current mutation source: production multiline session/defaults, both `cedar-media` ID forms, empty shortcuts, frame origin, parked v42, and fullscreen v42 all exist in source. The executable's generic failure text does not identify which runtime lookup returned `npos` | The red remains first and unresolved. Editing any locator would be speculative; this is not a second same-cause red because no unchanged or corrected rerun occurred | Stop this gate and request one bounded diagnostic-message disposition. Preserve all source/diff/gate evidence; do not proceed to later gates while parser admission is unresolved |
| 16 | Reviewer-authorized diagnostic setup | Authorization received to make the existing mutation helper print the exact missing locator and execute the same gate once | Diagnostic-only; no production, locator, or assertion semantic change | The next same-cause `npos` result is the mandatory second-red stop; an unexpected pass is preserved and explained |
| 17 | Diagnostic `WidgetBridgeCatalogTestsOnly` execution | Failed again with the exact lower-case `embedded media mutation source was absent`; the newly instrumented MediaViewport helper did not emit its capitalized message or locator | Second genuine red for the same missing-mutation-source issue. Exact-string lookup locates the emitted message at the separate bundle-boundary helper line 825. Its malformed matrix has two stale locators for a two-command list absent from the current four-command valid bundle | Mandatory stop. Smallest next correction, only if reviewer authorizes it: update the two bundle command-list mutation sources to the exact current four-command list and make each malformed replacement preserve all valid commands while adding only the duplicate or unknown command under test; remove the temporary diagnostic branch before eventual commit |
| 18 | Reviewer disposition after second-red stop | Explicit authorization received for the exact source-proven test-only bundle locator/replacement correction and removal of the temporary diagnostic branch | This authorization resumes only the stopped catalog gate; it does not erase the two-red evidence or authorize production/assertion broadening | Apply the exact change and run one replacement. Do not repeat already-green SDK or RichMedia contract gates |
| 19 | Reviewer-authorized `WidgetBridgeCatalogTestsOnly` replacement | Green: `WidgetBridge embedded media native boundary cases passed=13`; `WidgetBridgeCatalogTests passed` | Both stale command-list locators now match the current valid bundle and each malformed replacement adds only its intended duplicate or unknown command | Continue remaining narrow managed Bridge and package/widget gates |
| 20 | Managed Bridge exact authority selector setup | `dotnet run --no-build --no-restore` failed before execution because `tests\WidgetBridge.Tests\bin\Release\net8.0-windows10.0.19041.0\win-x64\WidgetBridge.Tests.exe` was absent | First genuine infrastructure red for this distinct apphost-materialization cause | Build the focused project serially without restore, then run the exact selector once; a repeated setup cause stops |
| 21 | Serialized no-restore WidgetBridge.Tests build | NETSDK1004: `tests\WidgetBridge.Tests\obj\project.assets.json` not found; 0 warnings, 1 error, 0.34 seconds | Second genuine red for the same fresh-worktree test-project materialization cause: neither restore assets nor the apphost exist | Mandatory stop. Smallest next action requires reviewer authorization: one serialized restore-bearing build with supported unrestricted network and NuGet auditing fully enabled, followed by the exact authority selector if green |
| 22 | Reviewer disposition after managed materialization second-red stop | Explicit authorization received for one restore-bearing Release build of only WidgetBridge.Tests, serialized, with supported network as required and NuGet audit enabled | Resumes only this blocked test-project materialization and its exact authority selector; no unrelated restore/build or repeated green gate | Run the recorded build once, then the exact authority selector once if green |
| 23 | Audited WidgetBridge.Tests materialization and exact authority selector | Restore-bearing serialized build green, 0 warnings/errors, 9.48 seconds. Exact selector then failed 0/1: line 627 expected `BridgeProtocolException`, but none was thrown | Fresh-worktree materialization is resolved. This is a distinct first behavioral red in a stale-authority subcase | Trace the exact changed authority and current Bridge admission contract; use at most one bounded owner-correct correction and exact replacement |
| 24 | Managed Bridge line-627 authority source audit | The failing subcase changed only `AspectRatio` from 16:9 to 4:3. `AdmitsEmbeddedMediaPlaybackEvent` continues to require an unconsumed captured authority, exact instance/session, exact command sequence/media key/payload, and `EmbeddedMediaResourceAuthority.Matches`; that resource owner compares session ID, entry asset, resources, frame origins, and domain families, deliberately not aspect ratio | Stale test oracle. WIDGE-178 defines aspect ratio as compatible mutable presentation authority, so no exception is the correct production behavior; the registry has not become permissive about durable document or pending-command authority | Rotate the fixture's exact entry asset and matching resource path for this rejection subcase, then restore them. This preserves a valid snapshot while exercising actual durable identity retirement. One exact replacement is permitted; the same authority cause red again requires a stop |
| 25 | Pre-replacement Bridge test audit | The bounded fixture edit is confined to one local `entryAsset` variable used by both `EntryAsset` and its matching resource path; the rejection snapshot remains valid and changes only durable document/resource identity. `git diff --check` remains clean apart from existing line-ending notices | Ready for the one serialized no-restore compilation and exact authority replacement permitted after attempt 23 | Run the exact recorded build once; if it compiles, run only `Embedded media resolution requires exact current publication authority` once |
| 26 | Serialized no-restore WidgetBridge.Tests compilation after the authority fixture correction | Green: all exact project references and WidgetBridge.Tests compiled; 0 warnings, 0 errors, 4.95 seconds | The bounded test correction is source-valid | Run the exact authority selector once; a same-cause behavior red is the second-red stop |
| 27 | Exact replacement managed Bridge authority selector | Green: `PASS Embedded media resolution requires exact current publication authority`; 1/1 tests passed | The stale aspect-ratio oracle was the only failure; strict durable resource and pending-command authority remains proven | Continue once to the provider-neutral two-adapter selector |
| 28 | Managed Bridge provider-neutral two-adapter selector | Green: `PASS Two provider-neutral media adapters resolve through one sealed contract`; 1/1 tests passed | Both sealed adapter families resolve through the unified session contract | Continue once to the built Embedded Media typed playback selector |
| 29 | Managed Bridge built Embedded Media selector | 0/1 before behavior: `UnauthorizedAccessException: Access to the path is denied` at `NamedPipeClientStream.ConnectAsync`; `WidgetBridgeServer` creates the private pipe with `PipeOptions.CurrentUserOnly` | First genuine red for a distinct restricted-execution named-pipe admission cause, not a media-session behavior result | Run the already-built exact selector once with unrestricted execution. No source/build change; the same access-denied result is the second-red stop |
| 30 | Unrestricted built Embedded Media replacement | Private-pipe admission succeeded; behavior then failed at Program.cs:5225 with `Expected '35', got '42'` | Distinct first genuine red for a stale direct-contract version oracle. The same method already asserts the initial snapshot uses `EmbeddedMediaSessionVersion` v42, and the post-command snapshot remains on that direct contract | Change only line 5226 from `SurfaceAppearanceVersion` to `EmbeddedMediaSessionVersion`, then compile and run the exact selector once. A repeat of this version cause stops |
| 31 | Serialized no-restore WidgetBridge.Tests compilation after version-oracle correction | Green: 0 warnings, 0 errors, 4.64 seconds | One-line test correction is source-valid | Run the already-built exact selector once unrestricted; no further same-cause correction without a stop |
| 32 | Exact unrestricted built Embedded Media replacement | Green: `PASS Built embedded media sample completes the typed playback loop`; 1/1 tests passed | The direct v42 session contract, command/terminal loop, provider-neutral native previous/next observations, and built sample path completed | Continue to one focused YouTube declaration/manifest invocation |
| 33 | Focused five-test YouTube gate setup | Failed before tests with NETSDK1004: `tests\YouTubeWidget.Tests\obj\project.assets.json` not found | First genuine red for a distinct fresh-worktree YouTube test materialization cause; no YouTube behavior executed | Replace once with the identical filtered gate but allow audited restore under supported unrestricted network access; same-cause failure stops |
| 34 | Audited restored five-test YouTube gate | Restore/materialization cleared; 4/5 passed. `RetainedVideoLinkRouteAndFullscreenStatesRemainValid` failed at line 206 because Link session `SupportedPresentations` still contains `OverlayFullscreen` | First genuine red for a distinct production declaration mismatch. `RenderPlayer` computes `showFullscreenAction = Route == Player`, uses it to omit the Link fullscreen control, but calls `CreateMediaSession` without that Boolean so its default `true` advertises authority the route does not expose | Pass only the existing predicate as the third argument. This makes Player fullscreen+compact and Link compact-only as already documented/tested; run the same five-test gate once |
| 35 | Five-test YouTube replacement after Link correction | Failed during implicit build with only `Build failed with exit code: 1`; no compiler diagnostic and no tests executed | Distinct pre-test build-engine symptom matching the already-proven parallel project-reference fan-out boundary; it is not a second Link behavior red | Use the established one serialized no-restore build, then exact `--no-build` selection. A repeated serialized failure at this boundary stops |
| 36 | Serialized no-restore YouTubeWidget.Tests build | Green: WidgetProtocol, WidgetSdk, YouTubeWidget, adapter conformance, and YouTubeWidget.Tests compiled; 0 warnings, 0 errors, 2.02 seconds | The implicit-build fan-out boundary is cleared and the Link correction is source-valid | Run the same five tests once without build/restore; no extra gate broadening |
| 37 | Post-build five-test `dotnet test` execution | No output after 2m47s. Read-only process audit found exact owner `dotnet.exe` PID 79060, responsive/idle test server PID 79240 at 0.140625 CPU, and child PID 61468; no matching Windows crash event | First genuine red for a distinct test-runner hang/transport boundary. It did not produce a Link behavior result and is not a second Link-capability red | Release only the exact task-owned harness tree after preserving identity, then execute the already-built apphost directly once with the identical filter; a repeated hang stops |
| 38 | Direct already-built YouTube apphost replacement | Green: five selected tests passed, 0 failed, duration 380 ms | The one-line Link declaration correction resolved attempt 34; no second Link behavior red. The `dotnet test` hang did not recur outside its server transport | Continue to the changed compact surface/render/accessibility native owners |
| 39 | Native `WidgetSurfaceTestsOnly` | Reached behavior and failed after 190 checks: `compact LT and RT targets use the declared step and clamp exact playback state` | First genuine red for a stale fixture setup. WIDGE-178 makes Compact media an explicit host selector alongside Full widget; this fixture pins the session but never cycles from default Full widget, so `CompactMediaSeekTarget` correctly has no compact authority | Add one `CycleLayout(1)` before `CommitSetup`, as established by adjacent explicit-layout fixtures. Preserve all production and expected target math; one exact replacement permitted |
| 40 | Replacement native `WidgetSurfaceTestsOnly` | The first compact target oracle cleared; execution advanced to 205 checks and failed `genuine media geometry replacement reconciles and notifies its owner exactly once` | Second genuine red from the same underlying stale fixture-selection issue. The scheduler fixture separately calls `Pin` then `CommitSetup` without `CycleLayout(1)`, so it remains on default Full widget; its compact playback observations and geometry successor cannot own compact-media notification semantics | Mandatory same-cause stop. Retain the first fixture correction and full worktree. Smallest reviewer-authorized next step would add only `scheduler.CycleLayout(1)` before `CommitSetup`, then run one exact replacement; no production or expected-math change |
| 41 | Reviewer-authorized post-stop compact fixture correction | Reviewer explicitly authorized adding only the analogous `scheduler.CycleLayout(1)` before scheduler setup and one exact replacement selector | Resumed only the stopped compact-surface gate; no production or expected-math change | Applied the one-line setup correction |
| 42 | Reviewer-authorized replacement native `WidgetSurfaceTestsOnly` | Green: `WidgetSurfaceCoordinatorTests passed (331 checks, pinned private working-set delta 0 bytes)` | Both stale explicit-Compact fixture constructions are corrected; no further same-cause red occurred | Continue only genuinely unverified focused owners |
| 43 | Native `PinnedSurfaceTestsOnly` | Reached behavior and failed after 3 checks: `page-event restart projects onto a host-monotonic sequence, fails closed at exhaustion, and retains command/media/origin authority` | First genuine red for a distinct stale source-oracle migration cause. Production retains the process monotonic counter and exact command/media/origin checks, but WIDGE-178 renamed the counter to `embeddedMediaSessionPlaybackEventSequence_` | Change only the two exact counter-name locator strings and run one exact replacement |
| 44 | Replacement native `PinnedSurfaceTestsOnly` | The counter oracle cleared; execution advanced to 4 checks and failed `retained media host contract section begins` | Second genuine red from the same stale retained-media source-oracle migration cause. The test still searches `main.cpp` for removed `struct EmbeddedMediaSession` and the old save/bind/transfer facade, while WIDGE-178 intentionally moved the session record and transition owner to `MediaSessionManager` | Mandatory stop. Preserve the one-line locator correction and all evidence. Reviewer disposition is required for one bounded test-only migration of this retained-media source-contract block to the current manager and exact-key host owners before any replacement or later gate |
| 45 | Reviewer-authorized post-stop retained-media source-oracle migration | Reviewer explicitly authorized replacing only the removed `main.cpp` session/save/transfer predicates with equivalent assertions against `MediaSessionManager` and exact-key host operations, preserving durable identity, endpoint transitions, compact input authority, and the single lifetime owner | Resumes only `TestAcceptedCompactMediaHostContract` and one exact `PinnedSurfaceTestsOnly` replacement | Migrated the obsolete block as one bounded test-only correction; a further red from this migrated host-contract cause stops |
| 46 | Reviewer-authorized replacement native `PinnedSurfaceTestsOnly` | The migrated retained-media block cleared; execution advanced from 4 to 47 checks and failed `fullscreen host contract section begins` | Another stale exact source-section locator in the same migrated host-contract family: `TestAcceptedOverlayFullscreenMediaHostContract` still anchors its controller branch on the pre-WIDGE-178 `if (OverlayFullscreenMediaRequested())` shape, while fullscreen input now resolves the exact manager overlay endpoint/session owner | Mandatory stop under the explicit disposition. No correction or later gate. Reviewer decision is required before migrating this separate fullscreen source slice to its current exact-key manager-owned boundary |
| 47 | Reviewer-authorized post-stop fullscreen host-contract migration | Reviewer explicitly authorized migrating the entire bounded `TestAcceptedOverlayFullscreenMediaHostContract` function in one pass, preserving authority/security/input/escape/geometry behavior while removing only deleted facade/source-shape predicates | Resumes one exact `PinnedSurfaceTestsOnly` replacement; a further red in this migrated function stops | Replaced the old controller, paint, geometry, and committed-presentation source anchors with current exact session request, manager overlay endpoint, exact-key controller route, aspect-fit geometry, and immediate exit-settlement checks |
| 48 | Reviewer-authorized replacement native `PinnedSurfaceTestsOnly` | The migrated fullscreen function compiled and execution advanced to 56 checks, then failed `fullscreen routes B, X, LT, and RT while View cannot reach hidden tray routing` | Required stop in the migrated fullscreen function. Exact remaining assertion is the inherited `fullscreenInput.find("XINPUT_GAMEPAD_BACK") == npos`: the current slice intentionally begins at `constexpr WORD recoveryChord = XINPUT_GAMEPAD_BACK | XINPUT_GAMEPAD_START`, so token absence no longer proves routing isolation. The fullscreen branch still returns before `pinnedControllerCommand`/tray handling | Mandatory stop. No correction or later gate. Smallest future test-only disposition is to replace only this obsolete token-absence predicate with an ordering/branch-return assertion proving fullscreen handling terminates before pinned/tray View routing |
| 49 | Reviewer-authorized fullscreen View-routing predicate correction | Reviewer explicitly authorized replacing only the impossible `XINPUT_GAMEPAD_BACK` token-absence assertion with the actual terminal-routing invariant | Resumes one exact `PinnedSurfaceTestsOnly` replacement; a repeat of this semantic replacement red stops | The test now locates the exact fullscreen controller branch, its terminal return, and the later `pinnedControllerCommand` construction, requiring the fullscreen branch to terminate first |
| 50 | Replacement native `PinnedSurfaceTestsOnly` after fullscreen View correction | The exact fullscreen routing invariant cleared; execution advanced from 56 to 125 checks and failed `media Back shortcut requires exact overlay media and widget-owned focus authority` | First genuine red for a distinct stale media-Back source oracle. Production now derives the same authority from manager `EndpointOwner(Overlay)`, exact session lookup, `EmbeddedMediaAuthorityCurrent`, non-Parked presentation, and widget-owned focus; the test still names deleted `embeddedMediaAuthority_` projection fields | Replace only those obsolete authority locators with the current exact manager/session predicates, then run one exact replacement; a same-cause red stops |
| 51 | Exact replacement native `PinnedSurfaceTestsOnly` after media-Back locator migration | Failed again after 125 checks at the identical `media Back shortcut requires exact overlay media and widget-owned focus authority` assertion | Second genuine red from the same media-Back source-oracle cause. Exact construction defect: `mediaBackBranch` is sliced from `const bool overlayMediaAuthorityCurrent`, while the newly asserted `mediaSessions_.EndpointOwner(Overlay)` and `mediaSessions_.Find(*overlayKey)` statements occur immediately before that offset and therefore cannot be found in the substring | Mandatory stop. No correction or later gate. Smallest reviewer-authorized next step would widen only the test slice to start at the exact `overlayKey` declaration (or test those two predicates against `mediaBack`) while preserving every semantic authority assertion, then run one replacement |
| 52 | Reviewer-authorized post-stop media-Back slice correction | Reviewer explicitly authorized widening only the inspected subsection to the exact `overlayKey` declaration while retaining every manager/session/presentation/widget-focus assertion | Resumes one exact `PinnedSurfaceTestsOnly` replacement; a repeat of this slice/authority cause stops | Added the exact owner-start locator, required it to precede authority assembly and routing, and based the retained semantic checks on that bounded subsection |
| 53 | Reviewer-authorized replacement native `PinnedSurfaceTestsOnly` | Green: `PinnedSurfaceHostTests passed (449 checks, private working-set delta 1150976 bytes, idle CPU 0%, semantic p95 0.0003 ms)` | The widened slice proves the exact manager overlay owner, exact current session, non-Parked presentation, widget-owned focus, fullscreen precedence, and generic B routing | Continue only genuinely unverified direct owners |
| 54 | Native `DeclarativeRendererTestsOnly` | Green: `DeclarativeRendererTests: 5090 checks passed` | Direct media viewport rendering and geometry consumers compile and pass | Continue to direct accessibility owner |
| 55 | Native `AccessibilityTreeTestsOnly` | Green: `AccessibilityTreeTests passed (26 checks)` | Media accessibility projection remains valid | Continue to the directly changed CLI scenario/template consumers |
| 56 | Exact WrailCli scenario-preview selector setup | `dotnet run --no-build --no-restore` failed before behavior because `tests/WrailCli.Tests/bin/Release/net8.0/WrailCli.Tests.exe` does not exist | First genuine infrastructure red for a distinct fresh-worktree WrailCli materialization cause | Run one serialized no-restore build, then the exact selector only if compilation succeeds |
| 57 | Serialized no-restore WrailCli.Tests build | NETSDK1004: `tests/WrailCli.Tests/obj/project.assets.json` not found; 0 warnings, 1 error, 0.32 seconds | Second genuine red for the same fresh-worktree WrailCli materialization cause; the missing apphost and missing restore assets are one setup boundary and no test behavior ran | Mandatory stop. Reviewer authorization is required for one restore-bearing serialized WrailCli.Tests build with supported unrestricted network access and NuGet audit enabled, followed by the exact scenario selector if green |
| 58 | Reviewer-authorized post-stop WrailCli materialization | Reviewer explicitly authorized one audited network-enabled serialized Release build for only WrailCli.Tests, then only its directly affected built-output selectors | Resumes the missing-assets setup boundary; same-cause recurrence stops, while a distinct behavior red starts its own count | Planned selectors are exact scenario-preview media projection and exact embedded-media template lifecycle/adapter contract; no unrelated CLI aggregate coverage |
| 59 | Audited network-enabled serialized WrailCli.Tests build | Green: restore completed, every exact dependency and WrailCli.Tests compiled; 0 warnings, 0 errors, 4.92 seconds | Missing assets/apphost materialization is resolved with NuGet auditing enabled | Run exact affected selectors against the built output |
| 60 | Exact WrailCli scenario-preview selector | Reached behavior and failed 0/1 with only `Expected '0', got '1'` | First genuine red for a distinct scenario-subprocess behavior/admission cause; the method contains four successful-exit assertions before its changed in-process media placeholder, so the emitted result did not identify the failed stage | Direct built CLI `preview help` was green. Add bounded test-only context to those four exit assertions and run one exact diagnostic replacement; a repeated scenario failure stops |
| 61 | Serialized no-restore WrailCli.Tests build after bounded diagnostic | Green: 0 warnings, 0 errors, 2.78 seconds | Diagnostic-only test change compiled | Run the same exact scenario selector once |
| 62 | Exact diagnostic replacement WrailCli scenario-preview selector | Failed 0/1: `ordinary scenario preview failed: error: The isolated scenario worker failed safely. Expected '0', got '1'.` | Second genuine red for the same scenario-worker behavior/admission issue. Help and manifest-only listing precede this point; the failing boundary is the first isolated provider execution, before the changed in-process media-placeholder assertion. The production command intentionally sanitizes WidgetProcess/Admission/Protocol/UnauthorizedAccess exceptions to this message | Mandatory stop. Retain bounded diagnostic context. Reviewer disposition is required to distinguish restricted AppContainer/process admission from a runtime behavior red, preferably via one unrestricted execution of the already-built exact selector; no source correction is proven |
| 63 | Reviewer-authorized unrestricted execution of the already-built exact WrailCli scenario selector | Green: `PASS Scenario manifests are bounded and isolated execution is declared`; 1/1, exit code 0 | The unchanged test and binaries pass when process/AppContainer admission is unrestricted, conclusively classifying attempts 60 and 62 as environment-only rather than WIDGE-178 behavior failures | Continue to the still-unverified embedded-media template and parity gates without rebuilding the WrailCli test binary |
| 64 | Exact embedded-media template end-to-end gate | Green: `PASS Embedded media template end to end`; exit code 0 | Isolated scaffold creation, audited restore/build, generated tests, preview validation, adapter conformance, validation, and package contents all completed | Continue to canonical managed/native protocol parity |
| 65 | Canonical managed/native protocol parity verifier | Green: 110 managed constants, 1 shortcut-repeat rule, and 4 owner-availability rules match the native Release artifact exactly | Missing, added, renamed, and changed-value drift fail closed | Stop at the reviewer-authorized template/parity boundary; final coherent Release/build, audit, stage, and commit remain pending disposition |
| 66 | Reviewer-dispatched final coherent audited `Release -SkipTests` build | Green: restore was current with NuGet auditing enabled; native host and all managed runtime publications compiled; Settings and first-party packages materialized; Embedded Media sample sealed; `OverlayHost.exe` produced | Four pre-existing C4244 unsigned-to-float conversion warnings were emitted in `main.cpp`; no error occurred | No additional package gate remains unverified: YouTube declaration/manifest v0.3.14 is already green 5/5 and the changed Embedded Media sample/package was built and sealed here. Proceed to final scope/diff/staged review and local commit without repeating focused gates |
| 67 | Reviewer source review of `MediaSessionManager::Reconcile` | Cross-endpoint `Present` set only the destination endpoint owner and never released the source. A session moving `OverlayViewport` to `CompactPinned` stayed recorded as the overlay owner, so the next unrelated overlay presentation took the displaced-owner branch and parked the live compact-pinned session | Correctness defect against the locked decision that a compact-pinned session and a different overlay session own distinct endpoints simultaneously | Added `MediaSessionManager::ReleaseOtherEndpointOwnership` and called it at the single ownership-acquisition point. Reproduced first as a failing contract case (`moving to the pinned endpoint releases the overlay endpoint`), then green |
| 68 | Reviewer source review of `MediaSessionManager::Reconcile` | `Reconcile` held a `SessionRecord*` across every `operations.*` side effect. `RichMediaSurfaceCoordinator::{SetVisible,BeginPresentationTransfer,CompletePresentationTransfer}` call `Fault`, which synchronously runs `configuration_.invalidate` to `OnRichMediaStateChanged`, which calls `StopEmbeddedMediaSession` and erases the record. The failure paths then ran `operations.fault(key, *current)` on the freed record | Use-after-free reachable from any faulting presentation, park, or update side effect | Re-resolve the record after every side effect, treat a vanished entry as the completed terminal transition, and hold a `shared_ptr` to the coordinator (and to the displaced session coordinator) so the controller cannot be destroyed while its own member function is on the stack. Same hardening applied to `ExecuteRetireEmbeddedMediaSession` and the two `WM_SIZE`/`WM_DPICHANGED` overlay-owner hide paths. Reproduced first as a failing contract case (`the re-entrant retirement runs once and is not repeated on the erased record`), then green |
| 69 | Reviewer source review of `MediaSessionManager::Reconcile` and `ExecuteEmbeddedMediaPresentationTransfer` | The `None` effect adopted `input.desiredGeometry` even when the plan target was `Parked`, which defeated the parked no-op guard and replanned a redundant `Park` on every following reconcile; `ExecuteEmbeddedMediaPresentationTransfer` dereferenced `CurrentMediaViewport(...)` without checking the optional when stamping `pinnedFrameGeneration` | Idempotence defect plus one unguarded optional dereference | `None` now adopts geometry only for a presenting target; the pinned frame generation resolves the committed viewport once and fails the transfer closed when it is absent |

## Decision and scope-change log

Append any implementation-specific decision before relying on it.

| Date | Decision | Reason | Scope impact |
| --- | --- | --- | --- |
| 2026-09-03 | Choose Option B: in-process multi-session manager | One lifetime/presentation owner removes the disconnected overlay, fullscreen, pin, and parking behavior | Direct SDK, Bridge, host, YouTube, and sample integration |
| 2026-09-03 | Shared environment, per-session controller/document | Supports multiple independent widgets without multiplying WebView2 environment ownership | Manager owns a bounded session registry |
| 2026-09-03 | No compatibility layer or exhaustive transition suite | Explicit user direction; old declarations are internal to the repository and can be replaced directly | Remove obsolete API/tests/docs instead of adapting them |
| 2026-09-03 | Start from accepted `main`; do not cherry-pick WIDGE-177 tail | The tail is a rejected special-case fix and conflicts with unified ownership | Preserve it only as investigation evidence |
| 2026-09-03 | Use exact dispatch baseline `dfc43f6a8a98ca83f74940785edd4e937de2a746` | Plane's latest implementation-dispatch comment and direct assignment supersede the older baseline still present in the Plane body and initial plan | New isolated branch/worktree created directly from `dfc43f6a`; no cherry-pick or reuse of WIDGE-177 evidence |
| 2026-09-03 | Use the preferred direct public names unchanged | Current vocabulary makes `EmbeddedMediaSession` and `SupportedPresentations` precise and non-conflicting | Protocol/SDK/native/package consumers change once with no alias |
| 2026-09-03 | Exclude `docs/implementation-status.md` | Reviewer scope correction reserves all reviewer-owned status/planning/history documents; this living plan is the sole exception | Removed from changed-file ledger before any edit; public authoring/SDK docs remain in scope |
| 2026-09-03 | Preserve compact controller semantics as locked behavior | Reviewer checkpoint restates the accepted host-owned contract | X toggle; LT/RT declared-step seek; conditional LB/RB previous/next; B compact-interaction exit; View tray return, all keyed to exact `CompactPinned` session |
| 2026-09-03 | Resume after compaction without repeating inventory or gates | Full plan read plus HEAD/branch/status/log/diff audit found the retained work exactly at the recorded Phase 1 boundary | Continue from the 33-path unstaged contract migration; no staging, build, test, package operation, red, or external mutation occurred |
| 2026-09-03 | Represent parking by declaration plus viewport absence in every validator/host compatibility branch | This is the locked direct contract and removes the route-specific retention Boolean | Cold zero-viewport declarations fail without creating a controller; an exact resident session parks and remains eligible for later reattachment |
| 2026-09-03 | Remove fullscreen-state notification transport | Inventory found no production widget consumer; fullscreen is host presentation state, not widget authority | SDK callback/context property, runtime/Bridge validation, native JSON field, and host notification dispatch removed |
| 2026-09-03 | Apply the corrected same-issue second-red stop rule | Direct user correction: the first genuine red for one proven cause permits one bounded owner-correct change and exact replacement gate; the second genuine red from that same cause stops the assignment, while an evidence-distinct cause begins its own count | Gate and correction decisions will be tracked by root cause; no red has occurred so far |
| 2026-09-03 | Resume Phase 2 after compaction without repeating green gates | Full living plan plus HEAD/branch/status/log/diff audit found exact baseline HEAD and 38 retained paths, including the two new manager files; nothing is staged | Canonical generator and compile-only WidgetSdk evidence remain authoritative; manager integration resumes at the partially migrated `main.cpp` boundary |
| 2026-09-03 | Reject the provisional mutable selected-session manager facade before integration | Reviewer architecture correction found `activeKey_`, widget-first selection, no-key accessors, detached fallback, and parallel projection/parked/presentation state recreate the old ownership bug under a new name | Remove that API and state before integrating call sites; use exact `SessionKey` operations/callback captures, one `PresentationState`, manager-owned overlay/pinned arbitration and ordered execution, and fail-closed invalid geometry. Compact/fullscreen input must also revalidate exact key plus presentation generation |
| 2026-09-03 | Split durable document identity from mutable presentation/command authority | `SameEmbeddedMediaResourceContract` currently includes size, aspect, supported presentations, seek step, and commands, which would incorrectly recreate a live document for ordinary authority changes | Add a narrow sealed document/resource identity comparison for lifetime; update mutable declaration/commands in place after exact-current admission |
| 2026-09-03 | Make Compact media an explicit host-provided pinned selector choice | The current `compactMediaPresentation()` path implicitly reinterprets every authored pinned layout as compact media whenever supported | Add one distinct Compact media selector entry beside authored layouts while reusing the one pinned HWND and existing pinned lifecycle owners |
| 2026-09-03 | Restore `main.cpp` to its pre-manager mechanical state before exact-key integration | A prior broad accessor substitution produced invalid declarations and would have compounded the rejected selected-session design | Mechanically reversed only the four provisional accessor spellings; all earlier direct contract, callback-removal, parser, and harness-removal edits remain intact |
| 2026-09-03 | Treat a failed post-write replacement-count guard as tooling construction evidence, not a genuine gate red | The guarded rename completed correctly, but its expected count double-counted the old name as a substring of the comparison helper and threw after writing | Verified the intended symbol migration separately; no compiler/test/product gate ran and the WIDGE-178 red count remains zero |
| 2026-09-03 | Harden the explicit Compact media selector boundary before its first gate | Independent review found duplicate Pin/Update catalog construction, an undisambiguated authored `host.compact-media` collision, Full Widget sizing still overwritten by media preferred size, stale committed geometry exposed while dirty, unconditional compact accessibility claims, and hidden placement chrome | Use one catalog builder with exact host-ID collision rejection; keep ordinary Full Widget extent; expose pending geometry while dirty; derive shortcut help from declared commands; keep visible host setup chrome above the video plane |
| 2026-09-03 | Model same-presentation endpoint geometry changes inside the manager transition executor | Reviewer P1 found that enum-only `PlanTransition` returned `None` for changed bounds/clip/DPI or committed geometry, which would either leave stale placement or force geometry mutation around the single executor | Add typed current/desired endpoint geometry plus revision, an idempotent `Update`/retarget effect, and commit geometry only after its exact-key operation succeeds; equivalent telemetry remains `None` and composition dedupe remains authoritative |
| 2026-09-03 | Restrict registry erasure to completed terminal effects | Public `Remove`, `RemoveForWidget`, and `Clear` could erase an attached controller without ordered detach/retire | Replace raw public erasure with manager-owned exact-key retire operations; any internal erase occurs only after the supplied terminal executor succeeds |
| 2026-09-03 | Resume after the latest compaction at the partially migrated shutdown boundary | Full living-plan, branch, HEAD, status, log, diff, manager, old-symbol, and shutdown audit found 40 retained paths, nothing staged, exact baseline HEAD, and the shutdown patch present | Preserve the two green Phase 1 gates and continue the current Phase 2 core slice; no gate, process, package, external, or red-count mutation occurred during recovery |
| 2026-09-03 | Separate pinned frame freshness from presentation-data changes | Reviewer follow-up found that comparing every committed frame generation would convert equivalent pinned repaints into endpoint Updates | Endpoint equality ignores the freshness counter; an equivalent reconcile advances the committed authority token without a composition side effect. Update is eligible only while the exact key still owns that endpoint; absent/mismatched ownership routes through Present/arbitration, and a failed Update faults/retire-clears the exact session rather than trusting partial visual state |
| 2026-09-03 | Complete the direct-break session vocabulary and widget semantics | Independent contract review found stale widget-facing `SessionId` vocabulary, template booleans, package versions, and a YouTube early-park condition that depended on playback observation | Rename identity-bearing public/widget fields to `SessionId`/`MediaSessionId` while retaining “surface” only for geometry/composition; YouTube keeps one document identity, exposes fullscreen+compact only on loaded Player and compact-only on parked routes, retains early declared intent before telemetry, keeps authored pinned layouts empty, and advances immutable changed packages. Add only focused early-park and conditional compact command coverage |
| 2026-09-03 | Deduplicate already-parked reconciliation without losing typed reason authority | Manager review found endpoint-unavailable/Pending input would invoke the parking retarget on every parked-route snapshot | An exact already-Parked session with no committed endpoint geometry plans `None` for the same reason; a reason-only change updates typed authority without a WebView/composition effect; a still-attached session continues through ordered Park. Compatible committed presentation retention remains decided before dirty/Pending geometry can expose or detach stale coordinates |
| 2026-09-03 | Keep same-endpoint geometry and visibility commits inside the manager executor | Independent pre-gate review found the provisional Update callback returned success through the same-source/destination transfer fast path, after which the manager committed desired geometry before an ignored out-of-band visibility/geometry reconcile | Extend desired endpoint state with controller-local geometry and visibility; make the exact-key Update callback perform geometry, composition, and reveal/hide ordering and return the real HRESULT; remove unconditional post-manager endpoint mutation. This is a pre-test source defect, not a genuine red |
| 2026-09-03 | Gate every global media endpoint and accessibility mutation by exact manager ownership | Callback review found delayed visibility/retirement work could derive an endpoint from stale per-session presentation, with Parked falling through to Overlay, and could clear another session's accessibility root | Before any overlay/pinned visual mutation require `EndpointOwner(endpoint) == exactKey` plus matching committed presentation/geometry; track and clear accessibility only for its exact owner. A transfer-pending Park returns `E_PENDING` rather than allowing the manager to commit Parked before detach completes. This is pre-test source review, not a red |
| 2026-09-03 | Admit a cold zero-viewport declaration as dormant rather than failed | Protocol v42 deliberately makes zero viewports a valid parked declaration, while the provisional host branch still recorded a widget failure when no exact controller existed | Do not instantiate a cold background controller and do not fail the widget; keep compact selector/action unavailable until an exact resident controller exists, with at most bounded debug evidence |
| 2026-09-03 | Decouple parked command authority from presentation transfer eligibility | The inherited parked-to-overlay transfer guard rejected any current declaration carrying a pending command, even though command authority is mutable and separately correlated | Remove the pending-command transfer rejection; advance exact current command authority and dispatch/terminalize it through its existing owner without treating it as document or presentation identity |
| 2026-09-03 | Remove enum-only transfer success and classify pinned retirement explicitly | Manager recovery can convert Update to Present when endpoint ownership is absent, but the inherited transfer shortcut returned success on matching presentation enum without attaching; pinned HWND stop reasons were also mostly treated as terminal | Present always executes its ordered attach/retarget path; manager alone owns dedupe. Non-terminal Unpin/Close/DisplayUnavailable/EmergencyHide/recreation returns to eligible OverlayViewport or Parked, while widget/runtime/terminal-worker/host/coordinator stops retire. Pending detach cannot be reported retained while the source HWND is destroyed |
| 2026-09-03 | Keep fullscreen intent durable until an explicit exit or authority retirement | The provisional manager cleared `presentationRequest` after successful Present, making the next telemetry snapshot select ordinary overlay and recreating one-frame fullscreen churn | A fulfilled fullscreen request remains desired/current across compatible snapshots; explicit B/exit clears it before reconciling OverlayViewport, while capability loss, pinned takeover, runtime/session retirement, and terminal lifecycle clear it through their existing exact owners |
| 2026-09-03 | Use durable fullscreen intent in pre-commit compatible-snapshot retention | Before the successor layout commits, deriving only CompactPinned or OverlayViewport makes an existing fullscreen presentation appear mismatched and hides it between telemetry admission and paint | The compatible pre-commit branch derives OverlayFullscreen from the exact retained presentation request, keeps the committed fullscreen plane visible/inert, and lets the post-frame manager update publish successor geometry |
| 2026-09-03 | Bind Bridge command terminals to durable document identity rather than mutable presentation metadata | `BridgeClientRegistry` terminal correlation currently includes surface/aspect, commands, supported presentations, and seek step, so an otherwise valid in-flight command can be stranded by a compatible presentation-only update | Correlate with the exact durable document/resource identity plus exact session and command authority; retain current-authority re-admission without treating mutable presentation fields as document replacement |
| 2026-09-03 | Make shared WebView environment completion outlive the initiating session lease | Environment creation is shared process state, but the initiating session's `CallbackLease` can be retired before the shared callback resolves, leaving the shared lifecycle stuck in `Creating` and waiters stranded | The shared environment owns completion/failure publication and wakes all waiters; initiating-session teardown may not suppress the shared terminal transition, and future sessions remain recoverable |
| 2026-09-03 | Resume the thirteen-item source slice after compaction | Full goal/plan read and exact worktree audit found baseline HEAD unchanged, 61 tracked paths plus three untracked files, nothing staged, and no new gate execution | Preserve all prior gate evidence; finish and independently review the retained partial manager/fullscreen/composition edits before the authorized serialized SDK and narrow native gates |
| 2026-09-03 | Bind compact UIA seek requests to the exact posting authority | The coordinator currently reduces a session-owned RangeValue action to a scalar target and the host later applies it to whichever session owns the pinned endpoint | Queue the exact session/layout/frame/action authority with the target and revalidate it at drain; a changed endpoint owner or projection rejects the request instead of seeking another session |
| 2026-09-03 | Keep compact View/B escape host-owned and terminal for the sampled frame | Transitional or absent media authority currently returns before escape handling, and a successful escape can still fall through to media commands from the same sampled frame | Process View/B before media-command authority checks and return immediately after either host transition; X/LB/RB/LT/RT remain gated by freshly validated exact compact-session authority |
| 2026-09-03 | Derive compact seek accessibility bounds from committed media/chrome geometry | The current accessibility slider spans the pinned HWND while the visible chrome is anchored to the aspect-fitted committed media region | Publish the exact committed media/chrome seek bounds to UIA and hit testing so the semantic target coincides with the rendered control across aspect fit and DPI |
| 2026-09-03 | Keep compact endpoint geometry in pinned-HWND pixels | The pinned coordinator authors and sizes its surface from `GetDpiForWindow`, but the host re-resolves compact geometry with owner DPI multiplied by global interface scale, double-scaling the WebView relative to its HWND | Compact media geometry conversion uses pinned HWND DPI/96 only; interface scale remains an overlay/content concern, while native chrome dimensions are scaled deliberately from their owning endpoint |
| 2026-09-03 | Retract the proposed extra held-seek owner | Source review proved `RichMediaSurfaceCoordinator::SendSeekPosition` admits no new seek while its command slot is pending, and the correlated playback acknowledgement advances authoritative position before clearing that slot | Keep LT/RT based on authoritative playback state; do not add optimistic state. The unused scrub helpers are outside this slice absent a separate proven defect |
| 2026-09-03 | Advance compatible parked-session authority without attaching | A hidden/cycled Player can publish a compatible successor while its durable session remains Parked; the current retain branch expects OverlayViewport, then clears geometry without advancing sequence, causing later command-free telemetry to fail current authority | When durable identity is exact and no endpoint/layout is current, keep the controller Parked, advance snapshot and mutable command authority, dispatch any exact pending command, and preserve the absent endpoint/geometry state |
| 2026-09-03 | Never retain dead endpoint ownership after irreversible retirement | Endpoint composition cleanup can fail after the coordinator is already torn down, while the manager currently retains the session and owner on any failed terminal callback | Terminal retirement clears manager endpoint ownership and registry after irreversible teardown; composition cleanup failure remains separately diagnosed rather than represented as live authority |
| 2026-09-03 | Fail closed after a partially applied Park effect | Parking mutates visibility/root target before late composition retirement can fail, so returning the HRESULT without changing manager state can leave the owner claiming a no-longer-presented controller | Direct and displacement Park failures invoke exact-session terminal cleanup and clear owner/registry; do not attempt an unprovable rollback of partially committed COM state |
| 2026-09-03 | Publish Present endpoint authority before synchronous attachment callbacks | `CompletePresentationTransfer` invokes its presentation-visibility callback before `operations.present` returns, while the retained manager code published endpoint owner/geometry afterward; the exact-owner guard therefore suppressed the attach-time callback | After any displaced owner is parked, publish the destination owner, presentation, geometry, and pinned frame authority immediately before `operations.present`; a failed effect still faults and erases the exact session. This is a pre-gate source-review correction, not a genuine red |
| 2026-09-03 | Terminalize immediate WebView environment creation failure through the shared owner | The asynchronous callback was moved to shared ownership, but the synchronous API-failure branch still erased/faulted only the initiating lease and could strand any waiter already enrolled in the shared Creating generation | Route the immediate failure through `CompleteSharedEnvironmentCreation` too; it transitions the shared generation to Cold and fails every live waiter exactly once. This is a pre-gate source-review correction, not a genuine red |
| 2026-09-03 | Stage hidden, then reveal the retargeted destination before retiring the source visual | The first staging correction kept destination composition hidden through source retirement, but moving the sole WebView root already blanks the old source; that order therefore introduced an avoidable blank interval | Keep attach-time callbacks hidden, then after successful root/parent/geometry attach enable the installed callback and commit the destination at the coordinator's actual visibility before retiring the now-inert source visual. A source-retirement failure still retires the destination and faults the exact session. This is a pre-gate source-review correction, not a genuine red |
| 2026-09-03 | Remove overlay interface scale from the compact geometry resolver itself | The later manager geometry record used pinned HWND DPI, but the shared presentation resolver had already converted CompactPinned bounds with `MediaPixelsPerDip(owner)`, which multiplies by global overlay interface scale | Resolve CompactPinned regions with `GetDpiForWindow(pinnedHwnd) / 96` at the conversion boundary; retain `MediaPixelsPerDip` only for ordinary/fullscreen overlay coordinates. This is a pre-gate source-review correction, not a genuine red |
| 2026-09-03 | Close the twenty-one-finding source audit before focused gates | Final static review confirmed the two last reviewer blockers are corrected: destination-visible commit precedes old-source retirement after hidden staging/attach, and CompactPinned geometry conversion uses pinned-HWND DPI without overlay interface scale. The retained Bridge, shared-environment, compact UIA/input, mutable geometry, fallback unpin, Park/Retire, and background authority slices have exact owners; `git diff --check` is clean | Begin only the authorized serialized SDK build, corrected SDK selector, and narrow manager/fullscreen gates. No optimistic held-seek state and no parallel SDK retry |
| 2026-09-03 | Keep provider-specific adapter conformance fixtures on their owning fake profile | `exercisePlaybackRateContract`, `exerciseMutedContract`, and `exercisePreferenceProviderErrors` manipulate only the state-callback player's state/callback/timer seams. Applying them to the HTML-media profile makes their state oracle impossible and would later test unrelated fake state instead of the HTML adapter. The preceding generic requirement loop already exercises every declared playback command for both profiles | Gate those three helper families on `profile === 'state-callback'`; do not weaken or remove the shared command/correlation/terminal exercise |
| 2026-09-03 | Use the actual DirectComposition opacity interface for hidden endpoint staging | `IDCompositionVisual2` owns the existing offsets/clip/tree operations but does not expose `SetOpacity`; Windows SDK `IDCompositionVisual3` is the typed visual opacity owner. Staging/reveal still requires opacity on the same created visual | Query the exact endpoint visual for `IDCompositionVisual3` at each changed commit, fail closed if unavailable, and keep all attachment/commit ordering unchanged. Qualify the manager test's shared-environment call with `widgetrail::richmedia` |
| 2026-09-03 | Admit the production-shaped MediaViewport checkpoint at the direct-break version | The fixture was renamed to `embeddedMediaSession` and `mediaSessionId` but retained protocol v41. Both managed and native contracts require v42 for the new session declaration, so the production parser correctly rejected it before node-default assertions | Update only this fixture's checkpoint version to v42; retain its ordinary/recovery defaults and incremental materialization assertions |
| 2026-09-03 | Count genuine reds by underlying issue/error | User correction: the implementation stops at the second genuine red from the same underlying cause, not merely the second red in a gate. A distinct root cause starts its own count | The first same-cause red permits one bounded owner-correct correction and one exact replacement; a repeated same-cause red stops and preserves evidence |
| 2026-09-03 | Preserve committed compact-media geometry across a pinned resize until a replacement frame succeeds | Physical evidence: growth repaints incidentally, while shrink can leave the coordinator dirty because `WM_SIZE` resizes/discards resources without invalidating. `CurrentMediaViewport` then returns absent solely for dirty geometry; host reconciliation can interpret that as no compact endpoint. | Keep exact pinned/session/snapshot validation, retain the prior committed plane while geometry is pending, request one coalesced paint for every valid non-minimized resize, and replace/publish bounds only from successful `Paint`. Record bounded DEBUG resize/commit diagnostics; do not alter media lifetime, aspect fit, chrome, fullscreen, ordinary overlay, or placement limits. |
| 2026-09-03 | Route pinned-resize records through the runtime DEBUG action-correlation owner | Review found that compile-time `_DEBUG` removal produces no Release-candidate evidence, `controllerFocused_` is not controller visibility, and a lifetime 16-record cap can be exhausted before a physical shrink. | The coordinator now retains one coalesced pending resize through the next successful paint and exposes a fixed-capacity 16-record typed queue; the main owner drains it through `AppendActionCorrelation` at DEBUG severity, correlating the exact pinned session and actual rich-media lifecycle. Pin creation and every retirement clear extent/pending/queued records. |
| 2026-09-03 | Treat ordinary Player fullscreen admission and retained Link-player capabilities as independent WIDGE-178 regressions | A: `TryDispatchNativeMediaAction` requires the session authority sequence to equal the visible interaction snapshot, so a compatible telemetry successor rejects the reserved host action and falls through to the worker. B: `RenderPlayer` gates fullscreen/captions and its declaration on `Route == Player`, although Link delegates to that live player render. | Add a bounded host decision record and align native admission/execution with committed presentation authority without relaxing exact identity/current visible action checks; separately use the existing live-player predicate for Link controls/capability and carry an explicit Player-or-Link settings return route. Tests remain deferred until the next physical verdict. |
| 2026-09-03 | Keep Bridge terminal correlation durable across mutable aspect-ratio updates | The line-627 fixture expected a command terminal to become stale after changing only aspect ratio, contradicting the locked durable-document identity split | Keep production strict session/pending-command/resource matching unchanged. Test stale resource authority by rotating the exact entry asset and matching resource declaration instead |
| 2026-09-03 | Bind YouTube fullscreen capability to its existing live-player route predicate | Supersedes the earlier Player-only Link decision after physical evidence proved Link is a retained live player and must expose fullscreen/settings when video is available | Player and Link both declare OverlayFullscreen through the live-player predicate; Search/Settings remain parked and Link-originated nested settings return to Link. |

## Residual-risk ledger

Keep this short and current; resolved risks move to the decision or evidence logs.

| Risk | Mitigation/evidence | Status |
| --- | --- | --- |
| Partial composition transfer could leave a controller detached from every endpoint | Ordered executor with explicit Parked fallback and focused failure coverage | `[ ]` |
| A snapshot-sequence check could remain embedded in lifetime code | Inventory and remove exact-sequence lifetime ownership; retain sequence only for stale actions/commands/layout observations | `[ ]` |
| Shared-environment failure could affect all sessions | Distinguish per-controller exit from shared-environment failure and preserve existing recovery policy | `[ ]` |
| Endpoint contention could destroy a non-winning session | Arbitration parks the non-winner and preserves its controller/document. Reviewer attempt 4 found and fixed the stale source-endpoint owner that made this fire against a live compact-pinned session; covered by a contract case | `[x]` |
| A synchronous controller fault during a manager side effect could erase the record under the reconciler | Reviewer attempt 5 re-resolves the record after every side effect and pins the coordinator lifetime for the duration; covered by a contract case | `[x]` |
| Direct protocol change could leave an old consumer compiling through stale generated output | Canonical generation plus repository-wide symbol search for removed names | `[ ]` |
| A fifth session could silently evict active playback | Explicitly reject fifth admission and leave all four resident sessions intact | `[ ]` |
| Compact media could remain a parallel pinned-window lifecycle | Reuse the existing pinned HWND, selector, placement, focus modes, opacity, persistence, and teardown; specialize only its session-backed content/chrome | `[ ]` |

## Reviewed source inventory to verify in Phase 0

This is a starting inventory, not permission to skip the repository-wide removed-symbol search.

Public managed contract and SDK:

- `src/WidgetProtocol/ViewModels.cs`
- `src/WidgetProtocol/ViewSnapshotValidator.cs`
- `src/WidgetProtocol/ProtocolVersionRequirements.cs`
- `src/WidgetProtocol/ProtocolConstants.cs`
- `src/WidgetProtocol/SnapshotJson.cs`
- `src/WidgetSdk/Widget.cs`
- `src/WidgetSdk/Elements.cs`
- `src/WidgetSdk/UI.cs`
- `src/WidgetSdk/WidgetPresentationDiff.cs`
- `src/WidgetSdk/PublicApi.txt`

Bridge, runtime, and generated/native wire propagation:

- `src/WidgetBridge/BridgeProtocol.cs`
- `src/WidgetBridge/EmbeddedMediaAssetResolver.cs`
- `src/WidgetBridge/BridgeClientRegistry.cs`
- `src/WidgetBridge/BridgeRequestClassification.cs`
- `src/WidgetBridge/WidgetBridgeServer.cs`
- `src/WidgetRuntime/RuntimeProtocol.cs`
- `src/WidgetRuntime/WidgetProcessClient.cs`
- `src/WidgetRuntime/WidgetWorkerServer.cs`
- `src/OverlayHost/WidgetBridgeClient.h`
- `src/OverlayHost/WidgetBridgeClient.cpp`
- `src/OverlayHost/WidgetProtocolPresentationContract.generated.h`
- `src/OverlayHost/EmbeddedMediaResourceContract.h`

Native manager/presentation ownership:

- new `src/OverlayHost/MediaSessionManager.h`
- new `src/OverlayHost/MediaSessionManager.cpp`
- `src/OverlayHost/main.cpp`
- `src/OverlayHost/RichMediaSurfaceCoordinator.h`
- `src/OverlayHost/RichMediaSurfaceCoordinator.cpp`
- `src/OverlayHost/OverlayCompositionSurface.h`
- `src/OverlayHost/OverlayCompositionSurface.cpp`
- `src/OverlayHost/DeclarativeRenderer.h`
- `src/OverlayHost/DeclarativeRenderer.cpp`
- `src/OverlayHost/WidgetSurfaceCoordinator.h`
- `src/OverlayHost/WidgetSurfaceCoordinator.cpp`
- pinned-surface host/placement consumers found by Phase 0
- `src/OverlayHost/build.ps1`, `src/OverlayHost/OverlayHost.vcxproj`, and applicable CMake/test registration

Direct widget and template consumers:

- `samples/YouTubeWidget/YouTubeVideoWidget.cs`
- `samples/YouTubeWidget/YouTubeVideoWidget.Search.cs`
- `samples/EmbeddedMediaWidget/EmbeddedMediaSampleWidget.cs`
- `templates/ControllerWidget/profiles/embedded-media.cs.template`
- `templates/ControllerWidget/tests/EmbeddedMediaScenarioTests.cs.template`
- `templates/ControllerWidget/README.embedded-media.md.template`
- `templates/ControllerWidget/template.json`

Direct documentation and focused-test families:

- `src/WidgetSdk/README.md`
- `docs/declarative-ui.md`
- `docs/embedded-media-widget.md`
- `docs/widget-quickstart.md`
- `docs/widget-authoring-guide.md`
- `docs/adding-declarative-ui-elements.md`
- focused SDK, Bridge, native manager, YouTube, Embedded Media, CLI preview, template, and package tests that fail to compile or directly assert the replaced contract

`MediaSessionsWidget` and Spotify/Windows media-session code are not embedded-WebView consumers and are outside this source change unless implementation discovers a real shared type dependency and records it first.

## Physical acceptance checklist

The implementation task prepares this checklist; only the reviewer/user performs it after installing the final candidate.

- [ ] Start a YouTube video, cycle to another widget, wait, and return. Playback/session continuity remains intact; Play resumes normally if provider policy paused it.
- [ ] Enter fullscreen and wait through multiple telemetry updates. Video remains fullscreen, the ordinary overlay does not redraw over it, and B exits exactly once to the same player.
- [ ] From Player, return to Search, open Settings, and return to Player. No retained-hidden error appears and the exact player/document resumes without reloading.
- [ ] Compact-pin the playing video, go to Search, unpin from the tray, and repin. No `presentation-transfer-detach`, retained-hidden, or stale-session error appears.
- [ ] Exercise compact pin while the owning widget is cycled away and back. The same session survives and controls remain current.
- [ ] Start media in a second widget while YouTube remains resident. Both sessions remain independent; presenting or closing one does not retire the other.
- [ ] Verify ordinary overlay controller input, fullscreen input, compact-pinned controls, Guide/B behavior, focus restoration, and media accessibility labels.
- [ ] In Compact media, verify `X` play/pause, `LT`/`RT` session-step seeking, conditional `LB`/`RB` previous/next, `B` click-through exit, and `View` tray return before and after an ordinary-layout switch and unpin/repin.
- [ ] Remove the media session declaration by ending/closing playback. Its controller is retired and no hidden undeclared playback remains.

## Final evidence and handoff

Populate at the end; do not claim manual acceptance here.

| Field | Final value |
| --- | --- |
| Implementation commit(s) | This single coherent WIDGE-178 commit; exact hash belongs in reviewer/Plane evidence rather than this commit body |
| Final tree | The complete WIDGE-178 tree recorded by this commit; exact tree hash belongs in reviewer/Plane evidence |
| Release artifacts | `src/OverlayHost/out/Release/OverlayHost.exe`; Bridge and WidgetWorkerHost runtime publications materialized |
| SHA-256 hashes | OverlayHost `F169EE0364E51F4B1A3ABE378D4B5CD1785F7389D84B080896DDE58FD09997B0`; WidgetBridge.exe `80719925CEF8D02F97E18256E9B0F2AC4814BCE3B9DCA27A00CB631333B3B4E6`; WidgetBridge.dll `3220EFF862EF49E902E92EA01F9C6BFCC8EA299BE7CB85F8D3722845C23B56FE`; WidgetWorkerHost.exe `9B0E3801A2C40154CBEB50F2A9358E325E9008856823086304F96E92A6E9FB26`; WidgetWorkerHost.dll `DBB6E30ED7F4BB753601543B14A0E19F12C0F9EDBAACE99FDCA54630C40298B7`; widget catalog `0FAF10999798939730B18AF1630B65B9993B8192649A6E1F58C023B37E915514` |
| Focused tests | Green: SDK media session 1/1; RichMedia manager contract 48; native Bridge boundary 13 plus catalog; managed Bridge exact authority/adapters/sample 1/1 each; YouTube selected 5/5; WidgetSurface 331; PinnedSurface 449; renderer 5090; accessibility 26; audited WrailCli build; unrestricted exact scenario selector 1/1; embedded-media template end to end; parity 110 constants/1 repeat rule/4 availability rules. The two restricted scenario failures are classified environment-only because the unchanged already-built selector passed unrestricted |
| Build/package result | Final coherent audited `Release -SkipTests` green on the post-correction tree; runtimes and bundled packages materialized, including the changed Embedded Media sample |
| Removed legacy symbols | Obsolete declaration booleans, SDK fullscreen callback/input context, mutable bound-session fields/helpers, and embedded handoff test owner are absent. Remaining `EmbeddedMediaSurface` names are geometry/composition types/functions, and protocol v22 remains historical version vocabulary rather than a compatibility declaration |
| Residual risks | Automated contract, authority, rendering, accessibility, template, package-materialization, parity, and coherent-build evidence is resolved. Manual multi-session presentation transfer, fullscreen, compact pin, route cycling, and teardown acceptance remains pending; no automated gate substitutes for that physical validation |
| Worktree clean | Yes at implementation handoff; the exact WIDGE-178 scope is contained in this commit with no staged, unstaged, or untracked residue |
| Manual gate | Pending reviewer/user acceptance |
