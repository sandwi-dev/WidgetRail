clock-widget {
  gap: 12px;
}

.clock-time {
  font-size: 48px;
  font-weight: 700;
}

.clock-date {
  color: var(--text-muted);
  font-size: 18px;
}

.primary-action:focused {
  outline-color: var(--accent);
  scale: 1.04;
}
