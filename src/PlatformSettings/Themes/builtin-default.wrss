:root {
  --canvas: #090908;
  --canvas-veil: rgba(0, 0, 0, 0.70);
  --surface: rgba(21, 21, 20, 0.98);
  --surface-raised: rgba(29, 29, 27, 0.98);
  --surface-subtle: rgba(39, 39, 36, 0.92);
  --surface-active: rgba(48, 47, 43, 0.98);
  --border: rgba(246, 240, 226, 0.12);
  --border-strong: rgba(246, 240, 226, 0.22);
  --text: #f2efe8;
  --text-muted: #aaa69d;
  --scrollbar-track: #34332f;
  --scrollbar-thumb: #aaa69d;
  --text-subdued: #7d7a72;
  --focus: #f4f0e8;
  --accent: #b8ae92;
  --success: #8eb49a;
  --info: #96aeb8;
  --warning: #c8a66a;
  --danger: #c98181;
  --poster-scrim: rgba(9, 9, 8, 0.82);
  --font-text: Segoe UI Variable Text, Segoe UI;
  --font-display: Segoe UI Variable Display, Segoe UI;
}

canvas {
  background: var(--canvas);
  color: var(--text);
  font-family: var(--font-text);
}
backdrop { background: var(--canvas-veil); }
scroll {
  scrollbar-width: 4px;
  scrollbar-track-color: var(--scrollbar-track);
  scrollbar-thumb-color: var(--scrollbar-thumb);
}
panel,
tray {
  background: var(--surface);
  color: var(--text);
  border-color: var(--border);
  border-width: 1px;
  corner-radius: 12px;
}
tray-item {
  min-width: 44px;
  min-height: 44px;
  background: transparent;
  color: var(--text-muted);
  corner-radius: 10px;
}
tray-item:selected { background: var(--surface-active); color: var(--text); }
tray-item:focused {
  outline-color: var(--focus);
  outline-width: 2px;
  outline-offset: -2px;
}

title {
  color: var(--text);
  font-family: var(--font-display);
  font-size: 28px;
  font-weight: 500;
  line-height: 1.12;
}
body {
  color: var(--text);
  font-family: var(--font-text);
  font-size: 15px;
  font-weight: 400;
  line-height: 1.35;
}
hint {
  color: var(--text-muted);
  font-family: var(--font-text);
  font-size: 13px;
  font-weight: 400;
  line-height: 1.3;
}

button {
  min-height: 44px;
  padding: 10px 14px;
  background: var(--surface-raised);
  color: var(--text);
  border-color: var(--border);
  border-width: 1px;
  corner-radius: 10px;
  font-family: var(--font-text);
  font-size: 15px;
  font-weight: 400;
  scale: 1;
  transition-duration: 90ms;
  transition-easing: ease-out;
}
button:selected {
  background: var(--surface-active);
  border-color: var(--border-strong);
}
button:focused {
  background: var(--surface-active);
  outline-color: var(--focus);
  outline-width: 2px;
  outline-offset: -2px;
  scale: 1;
}
select {
  min-height: 44px;
  padding: 10px 14px;
  background: var(--surface-raised);
  color: var(--text);
  border-color: var(--border);
  border-width: 1px;
  corner-radius: 10px;
  font-family: var(--font-text);
  font-size: 15px;
  font-weight: 400;
  scale: 1;
  transition-duration: 90ms;
  transition-easing: ease-out;
}
select:focused {
  background: var(--surface-active);
  outline-color: var(--focus);
  outline-width: 2px;
  outline-offset: -2px;
  scale: 1;
}
progress {
  min-height: 4px;
  background: var(--surface-active);
  color: var(--accent);
  corner-radius: 2px;
}
slider {
  min-height: 44px;
  padding: 8px 10px;
  background: var(--surface-subtle);
  color: var(--accent);
  border-color: var(--border);
  border-width: 1px;
  corner-radius: 10px;
  scale: 0.99;
  transition-duration: 90ms;
  transition-easing: ease-out;
}
slider:focused {
  background: var(--surface-active);
  outline-color: var(--focus);
  outline-width: 2px;
  outline-offset: -2px;
  scale: 1;
}

.wrail-scrubber {
  min-width: 0;
  gap: 2px;
  background: transparent;
  border-width: 0;
}
.wrail-scrubber__slider {
  width: 100%;
  min-width: 0;
  min-height: 44px;
  padding: 8px 4px;
  background: transparent;
  border-color: transparent;
  border-width: 1px;
  corner-radius: 8px;
}
.wrail-scrubber__slider:focused {
  background: var(--surface-subtle);
  outline-color: var(--focus);
  outline-width: 2px;
  outline-offset: -2px;
}
.wrail-scrubber__times {
  width: 100%;
  min-width: 0;
  gap: 8px;
  padding: 0px 4px;
  align: center;
}
.wrail-scrubber__elapsed,
.wrail-scrubber__duration {
  color: var(--text-muted);
  font-size: 12px;
  font-weight: 400;
  line-height: 1.2;
  max-lines: 1;
  text-overflow: ellipsis;
}
.wrail-scrubber__elapsed {
  flex-grow: 1;
  flex-shrink: 1;
}
.wrail-scrubber__duration {
  text-align: end;
  flex-shrink: 0;
}

.wrail-icon-button {
  width: 46px;
  height: 46px;
  min-width: 44px;
  min-height: 44px;
  padding: 10px;
  background: var(--surface-raised);
  color: var(--text);
  border-color: var(--border);
  border-width: 1px;
  corner-radius: 10px;
  flex-shrink: 0;
}
.wrail-icon-button--small { width: 44px; height: 44px; padding: 10px; }
.wrail-icon-button--large { width: 52px; height: 52px; padding: 12px; corner-radius: 12px; }
.wrail-icon-button--primary { background: var(--accent); color: var(--canvas); border-color: transparent; }
.wrail-icon-button--danger { background: var(--surface-raised); color: var(--danger); border-color: rgba(201, 129, 129, 0.34); }
.wrail-icon-button--quiet { background: transparent; color: var(--text-muted); border-color: transparent; }

.wrail-card {
  gap: 8px;
  padding: 12px;
  background: var(--surface-raised);
  border-color: var(--border);
  border-width: 1px;
  corner-radius: 10px;
}
.wrail-card--subtle { background: var(--surface-subtle); }
.wrail-card--transparent { background: transparent; border-width: 0; padding: 0; }

.wrail-section-header { gap: 4px; }
.wrail-section-header__content { gap: 10px; align: center; justify: space-between; }
.wrail-section-header__text { min-width: 0; flex-grow: 1; flex-shrink: 1; gap: 2px; }
.wrail-section-header__eyebrow {
  color: var(--text-muted);
  font-family: var(--font-text);
  font-size: 11px;
  font-weight: 500;
  letter-spacing: 0.03em;
  text-transform: none;
  max-lines: 1;
}
.wrail-section-header__title {
  color: var(--text);
  font-family: var(--font-display);
  font-size: 20px;
  font-weight: 500;
  line-height: 1.2;
  max-lines: 1;
  text-overflow: ellipsis;
}
.wrail-section-header__description {
  color: var(--text-muted);
  font-family: var(--font-text);
  font-size: 13px;
  font-weight: 400;
  line-height: 1.35;
  max-lines: 2;
}
.wrail-section-header__trailing { flex-shrink: 0; }

.wrail-badge {
  gap: 5px;
  align: center;
  padding: 4px 8px;
  background: transparent;
  color: var(--text-muted);
  border-color: var(--border);
  border-width: 1px;
  corner-radius: 10px;
}
.wrail-badge__icon { width: 14px; height: 14px; flex-shrink: 0; }
.wrail-badge__label {
  color: var(--text-muted);
  font-family: var(--font-text);
  font-size: 11px;
  font-weight: 500;
  max-lines: 1;
}
.wrail-badge__icon--info,
.wrail-badge__label--info { color: var(--info); }
.wrail-badge__icon--success,
.wrail-badge__label--success { color: var(--success); }
.wrail-badge__icon--warning,
.wrail-badge__label--warning { color: var(--warning); }
.wrail-badge__icon--danger,
.wrail-badge__label--danger { color: var(--danger); }

.wrail-divider { height: 1px; min-height: 1px; background: var(--border); }

.wrail-alert,
.wrail-empty-state {
  gap: 7px;
  padding: 12px;
  background: var(--surface-subtle);
  border-color: var(--border);
  border-width: 1px;
  corner-radius: 10px;
}
.wrail-alert__icon,
.wrail-empty-state__icon { width: 22px; height: 22px; color: var(--info); }
.wrail-alert__title,
.wrail-empty-state__title {
  color: var(--text);
  font-family: var(--font-display);
  font-size: 16px;
  font-weight: 500;
  line-height: 1.25;
  max-lines: 2;
}
.wrail-alert__message,
.wrail-empty-state__message {
  color: var(--text-muted);
  font-family: var(--font-text);
  font-size: 13px;
  font-weight: 400;
  line-height: 1.4;
  max-lines: 4;
}
.wrail-alert__action,
.wrail-empty-state__action { min-height: 44px; padding: 10px 14px; }
.wrail-alert__icon--success { color: var(--success); }
.wrail-alert__icon--warning { color: var(--warning); }
.wrail-alert__icon--danger { color: var(--danger); }

.wrail-segmented-tabs {
  gap: 2px;
  padding: 3px;
  min-height: 50px;
  background: var(--surface-subtle);
  border-color: var(--border);
  border-width: 1px;
  corner-radius: 10px;
  flex-shrink: 0;
}
.wrail-segmented-tabs__tab {
  min-width: 96px;
  min-height: 44px;
  padding: 9px 14px;
  background: transparent;
  color: var(--text-muted);
  border-color: transparent;
  border-width: 1px;
  corner-radius: 8px;
  flex-grow: 1;
}
.wrail-segmented-tabs__tab--selected {
  background: var(--surface-active);
  color: var(--text);
  border-color: var(--border);
}

.wrail-navigation-shell {
  width: 100%;
  min-width: 0;
  min-height: 0;
  flex-grow: 1;
  flex-shrink: 1;
  gap: 10px;
  overflow: clip;
}
.wrail-navigation-shell__compact {
  width: 100%;
  min-width: 0;
  min-height: 50px;
  flex-shrink: 0;
  gap: 2px;
  padding: 3px;
  background: var(--surface-subtle);
  border-color: var(--border);
  border-width: 1px;
  corner-radius: 10px;
  overflow: clip;
}
.wrail-navigation-shell__compact-item {
  min-width: 0;
  min-height: 44px;
  flex-grow: 1;
  flex-shrink: 1;
  gap: 7px;
  padding: 9px 10px;
  background: transparent;
  color: var(--text-muted);
  border-color: transparent;
  border-width: 1px;
  corner-radius: 8px;
  font-size: 12px;
  font-weight: 400;
  max-lines: 1;
  text-overflow: ellipsis;
}
.wrail-navigation-shell__body {
  width: 100%;
  min-width: 0;
  min-height: 0;
  flex-grow: 1;
  flex-shrink: 1;
  gap: 10px;
  align: stretch;
  overflow: clip;
}
.wrail-navigation-shell__rail {
  width: 156px;
  min-height: 0;
  flex-shrink: 0;
  gap: 4px;
  padding: 8px;
  background: var(--surface-subtle);
  border-color: var(--border);
  border-width: 1px;
  corner-radius: 10px;
  overflow: clip;
}
.wrail-navigation-shell__rail-item {
  width: 100%;
  min-width: 0;
  min-height: 44px;
  flex-shrink: 0;
  justify: start;
  gap: 9px;
  padding: 9px 11px;
  background: transparent;
  color: var(--text-muted);
  border-color: transparent;
  border-width: 1px;
  corner-radius: 8px;
  font-size: 12px;
  font-weight: 400;
  max-lines: 1;
  text-overflow: ellipsis;
}
.wrail-navigation-shell__compact-item:focused,
.wrail-navigation-shell__rail-item:focused {
  color: var(--text);
  outline-color: var(--focus);
  outline-width: 2px;
  outline-offset: -2px;
}
.wrail-navigation-shell__compact-item:pressed,
.wrail-navigation-shell__rail-item:pressed { opacity: 0.72; }
.wrail-navigation-shell__item--selected {
  background: var(--surface-active);
  color: var(--text);
  border-color: var(--border);
}
.wrail-navigation-shell__persistent {
  width: 320px;
  min-width: 240px;
  max-width: 360px;
  min-height: 0;
  flex-shrink: 1;
  overflow: clip;
}
.wrail-navigation-shell__content {
  min-width: 0;
  min-height: 0;
  flex-grow: 1;
  flex-shrink: 1;
  overflow: clip;
}

.wrail-switch {
  min-height: 44px;
  padding: 10px 14px;
  background: var(--surface-raised);
  color: var(--text-muted);
  border-color: var(--border);
  border-width: 1px;
  corner-radius: 10px;
}
.wrail-switch--on {
  background: var(--surface-active);
  color: var(--text);
  border-color: var(--border-strong);
}
.wrail-switch:focused {
  background: var(--surface-active);
  outline-color: var(--focus);
  outline-width: 2px;
  outline-offset: -2px;
}
.wrail-switch:disabled {
  background: var(--surface-subtle);
  color: var(--text-subdued);
  border-color: var(--border);
  opacity: 0.64;
}

.wrail-stepper {
  min-width: 0;
  gap: 10px;
  align: center;
}
.wrail-stepper__label {
  min-width: 0;
  color: var(--text);
  flex-grow: 1;
  flex-shrink: 1;
  max-lines: 1;
  text-overflow: ellipsis;
}
.wrail-stepper__value {
  min-width: 64px;
  color: var(--text-muted);
  text-align: center;
  flex-shrink: 0;
  max-lines: 1;
}
.wrail-stepper__button {
  width: 44px;
  height: 44px;
  min-width: 44px;
  min-height: 44px;
  padding: 10px;
  background: var(--surface-raised);
  color: var(--text);
  border-color: var(--border);
  border-width: 1px;
  corner-radius: 10px;
}
.wrail-stepper__button--decrement,
.wrail-stepper__button--increment { flex-shrink: 0; }
.wrail-stepper__button:focused {
  background: var(--surface-active);
  outline-color: var(--focus);
  outline-width: 2px;
  outline-offset: -2px;
}
.wrail-stepper__button:disabled {
  background: var(--surface-subtle);
  color: var(--text-subdued);
  border-color: transparent;
  opacity: 0.64;
}

.wrail-dialog {
  gap: 12px;
  padding: 16px;
  background: var(--surface);
  border-color: var(--border-strong);
  border-width: 1px;
  corner-radius: 12px;
}
.wrail-dialog__title {
  color: var(--text);
  font-family: var(--font-display);
  font-size: 20px;
  font-weight: 500;
  line-height: 1.2;
  max-lines: 2;
}
.wrail-dialog__content { gap: 8px; min-width: 0; min-height: 0; }

.wrail-settings-row {
  min-width: 0;
  gap: 7px;
  padding: 12px 2px;
  background: transparent;
  border-width: 0;
}
.wrail-settings-row__content {
  min-width: 0;
  gap: 10px;
  align: start;
}
.wrail-settings-row__icon {
  width: 20px;
  height: 20px;
  color: var(--text-muted);
  flex-shrink: 0;
}
.wrail-settings-row__copy {
  min-width: 0;
  gap: 3px;
  flex-grow: 1;
  flex-shrink: 1;
}
.wrail-settings-row__label {
  color: var(--text);
  font-size: 15px;
  font-weight: 500;
  line-height: 1.25;
  max-lines: 2;
  text-overflow: ellipsis;
}
.wrail-settings-row__description {
  color: var(--text-muted);
  font-size: 13px;
  font-weight: 400;
  line-height: 1.35;
  max-lines: 4;
  text-overflow: ellipsis;
}
.wrail-settings-row__metadata { min-width: 0; gap: 5px; padding: 0px 0px 1px 30px; }
.wrail-settings-row__value {
  color: var(--text-muted);
  font-size: 14px;
  font-weight: 400;
  line-height: 1.3;
  max-lines: 3;
  text-overflow: ellipsis;
}
.wrail-settings-row__status { align: start; }
.wrail-settings-row__action {
  min-height: 44px;
  padding: 10px 12px;
  background: transparent;
  color: var(--text);
  border-color: transparent;
  border-width: 1px;
  corner-radius: 8px;
  font-size: 14px;
  font-weight: 400;
  text-align: start;
}
.wrail-settings-row__action:focused {
  background: var(--surface-active);
  outline-color: var(--focus);
  outline-width: 2px;
  outline-offset: -2px;
}

.wrail-action-sheet {
  min-width: 0;
  min-height: 0;
  gap: 10px;
  padding: 16px;
  background: var(--surface);
  border-color: var(--border-strong);
  border-width: 1px;
  corner-radius: 12px;
}
.wrail-action-sheet__title {
  color: var(--text);
  font-family: var(--font-display);
  font-size: 20px;
  font-weight: 500;
  line-height: 1.2;
  max-lines: 2;
  text-overflow: ellipsis;
  flex-shrink: 0;
}
.wrail-action-sheet__description {
  color: var(--text-muted);
  font-size: 13px;
  font-weight: 400;
  line-height: 1.35;
  max-lines: 4;
  text-overflow: ellipsis;
  flex-shrink: 0;
}
.wrail-action-sheet__list {
  min-width: 0;
  min-height: 44px;
  max-height: 440px;
  gap: 4px;
  flex-grow: 1;
  flex-shrink: 1;
}
.wrail-action-sheet__item {
  min-height: 44px;
  padding: 10px 12px;
  background: transparent;
  color: var(--text);
  border-color: transparent;
  border-width: 1px;
  corner-radius: 8px;
  font-size: 15px;
  font-weight: 400;
  text-align: start;
  flex-shrink: 0;
}
.wrail-action-sheet__item--danger { color: var(--danger); }
.wrail-action-sheet__item:focused {
  background: var(--surface-active);
  outline-color: var(--focus);
  outline-width: 2px;
  outline-offset: -2px;
}

.wrail-picker {
  min-width: 0;
  min-height: 0;
  gap: 10px;
  padding: 16px;
  background: var(--surface);
  border-color: var(--border-strong);
  border-width: 1px;
  corner-radius: 12px;
}
.wrail-picker__title {
  color: var(--text);
  font-family: var(--font-display);
  font-size: 20px;
  font-weight: 500;
  line-height: 1.2;
  max-lines: 2;
  text-overflow: ellipsis;
  flex-shrink: 0;
}
.wrail-picker__description {
  color: var(--text-muted);
  font-size: 13px;
  font-weight: 400;
  line-height: 1.35;
  max-lines: 4;
  text-overflow: ellipsis;
  flex-shrink: 0;
}
.wrail-picker__options {
  min-width: 0;
  min-height: 44px;
  max-height: 440px;
  gap: 4px;
  flex-grow: 1;
  flex-shrink: 1;
}
.wrail-picker__option {
  min-height: 44px;
  padding: 10px 12px;
  background: transparent;
  color: var(--text);
  border-color: transparent;
  border-width: 1px;
  corner-radius: 8px;
  font-size: 15px;
  font-weight: 400;
  text-align: start;
  flex-shrink: 0;
}
.wrail-picker__option--selected { background: var(--surface-subtle); }
.wrail-picker__option:focused {
  background: var(--surface-active);
  outline-color: var(--focus);
  outline-width: 2px;
  outline-offset: -2px;
}

.wrail-value-row {
  min-height: 44px;
  gap: 12px;
  align: center;
  padding: 8px 2px;
  background: transparent;
  border-width: 0;
}
.wrail-value-row__icon {
  width: 20px;
  height: 20px;
  color: var(--text-muted);
  flex-shrink: 0;
}
.wrail-value-row__text {
  min-width: 0;
  gap: 2px;
  flex-grow: 1;
  flex-shrink: 1;
}
.wrail-value-row__label {
  color: var(--text);
  font-size: 15px;
  font-weight: 400;
  max-lines: 1;
  text-overflow: ellipsis;
}
.wrail-value-row__description {
  color: var(--text-muted);
  font-size: 13px;
  font-weight: 400;
  line-height: 1.3;
  max-lines: 2;
  text-overflow: ellipsis;
}
.wrail-value-row__value {
  color: var(--text-muted);
  font-size: 14px;
  font-weight: 400;
  text-align: end;
  max-lines: 1;
  text-overflow: ellipsis;
  flex-shrink: 0;
}

.wrail-choice-row {
  min-height: 44px;
  padding: 10px 12px;
  background: transparent;
  color: var(--text);
  border-color: transparent;
  border-width: 1px;
  corner-radius: 8px;
  font-size: 15px;
  font-weight: 400;
  text-align: start;
}
.wrail-choice-row--selected { background: var(--surface-subtle); }
.wrail-choice-row:focused {
  background: var(--surface-active);
  outline-color: var(--focus);
  outline-width: 2px;
  outline-offset: -2px;
}

.wrail-controller-hint {
  min-height: 24px;
  gap: 6px;
  align: center;
  flex-shrink: 1;
}
.wrail-controller-hint__key {
  min-width: 24px;
  padding: 3px 6px;
  background: var(--surface-active);
  color: var(--text);
  border-color: var(--border);
  border-width: 1px;
  corner-radius: 6px;
  font-size: 11px;
  font-weight: 500;
  text-align: center;
  max-lines: 1;
  flex-shrink: 0;
}
.wrail-controller-hint__label {
  color: var(--text-muted);
  font-size: 12px;
  font-weight: 400;
  max-lines: 1;
  text-overflow: ellipsis;
}

/* Non-modal notifications remain compact and presentational. Widgets own the
   lifecycle of a toast; the component never creates a focus stop. */
.wrail-toast {
  min-width: 0;
  gap: 10px;
  align: center;
  padding: 10px 12px;
  background: var(--surface-raised);
  border-color: var(--border);
  border-width: 1px;
  corner-radius: 10px;
  flex-shrink: 0;
}
.wrail-toast__icon {
  width: 20px;
  height: 20px;
  color: var(--text-muted);
  flex-shrink: 0;
}
.wrail-toast__icon--info { color: var(--accent); }
.wrail-toast__icon--success { color: var(--success); }
.wrail-toast__icon--warning { color: var(--warning); }
.wrail-toast__icon--danger { color: var(--danger); }
.wrail-toast__copy {
  min-width: 0;
  gap: 2px;
  flex-grow: 1;
  flex-shrink: 1;
}
.wrail-toast__title {
  color: var(--text);
  font-size: 14px;
  font-weight: 500;
  line-height: 1.25;
  max-lines: 2;
  text-overflow: ellipsis;
}
.wrail-toast__message {
  color: var(--text-muted);
  font-size: 13px;
  font-weight: 400;
  line-height: 1.3;
  max-lines: 3;
  text-overflow: ellipsis;
}

/* Rich tiles are one ActionSurface focus/hit target. Their descendants are
   presentation-only, so focus and pressed treatment always follows the full
   visible tile instead of an inner label. */
.wrail-action-surface {
  min-width: 0;
  min-height: 72px;
  gap: 12px;
  align: center;
  padding: 10px;
  background: transparent;
  border-color: var(--border);
  border-width: 1px;
  corner-radius: 10px;
  flex-shrink: 0;
}
.wrail-action-surface:focused {
  background: var(--surface-active);
  outline-color: var(--focus);
  outline-width: 2px;
  outline-offset: -2px;
}
.wrail-action-surface:pressed { background: var(--surface-raised); }
.wrail-action-surface:selected { border-color: var(--accent); }
.wrail-action-surface:disabled { color: var(--text-muted); }
.wrail-tile { flex-wrap: wrap; }
.wrail-background-surface {
  overflow: clip;
  background: rgba(18, 18, 20, 0.96);
  object-position: center;
}
.wrail-tile__artwork {
  width: 56px;
  height: 56px;
  corner-radius: 8px;
  flex-grow: 0;
  flex-shrink: 0;
}
.wrail-tile__content {
  min-width: 0;
  gap: 2px;
  flex-grow: 1;
  flex-shrink: 1;
}
.wrail-tile__title {
  color: var(--text);
  font-size: 15px;
  font-weight: 500;
  line-height: 1.25;
  max-lines: 2;
  text-overflow: ellipsis;
}
.wrail-tile__subtitle {
  color: var(--text-muted);
  font-size: 13px;
  font-weight: 400;
  line-height: 1.25;
  max-lines: 2;
  text-overflow: ellipsis;
}
.wrail-tile__metadata {
  color: var(--text-muted);
  font-size: 13px;
  font-weight: 400;
  line-height: 1.25;
  max-lines: 2;
  text-overflow: ellipsis;
}
.wrail-tile__state {
  color: var(--accent);
  font-size: 12px;
  font-weight: 500;
  max-lines: 1;
  text-overflow: ellipsis;
}

/* Poster tiles retain ActionSurface interaction semantics while the closed
   protocol-v37 presentation composes their optional Cover artwork behind this
   bounded bottom copy region. Aspect ratio, clipping, and typography remain
   theme-owned; text never determines the outer card extent. */
.wrail-poster-tile {
  min-width: 0;
  min-height: 0;
  aspect-ratio: 2/3;
  padding: 0px;
  gap: 0px;
  align: stretch;
  justify: end;
  overflow: clip;
  background: var(--surface-subtle);
  corner-radius: 12px;
}
.wrail-poster-tile:pressed { opacity: 0.78; }
.wrail-poster-tile__artwork {
  width: 100%;
  height: 100%;
  object-fit: cover;
  corner-radius: 12px;
}
.wrail-poster-tile__scrim {
  width: 100%;
  padding: 28px 14px 12px 14px;
  background: var(--poster-scrim);
}
.wrail-poster-tile__content { min-width: 0; gap: 4px; }
.wrail-poster-tile__subtitle {
  height: 18px;
  color: var(--text-muted);
  font-size: 11px;
  font-weight: 600;
  line-height: 1.2;
  max-lines: 1;
  text-overflow: ellipsis;
}
.wrail-poster-tile__title {
  height: 42px;
  color: var(--text);
  font-family: var(--font-display);
  font-size: 17px;
  font-weight: 600;
  line-height: 1.2;
  max-lines: 2;
  text-overflow: ellipsis;
}
.wrail-poster-tile__details {
  height: 18px;
  min-width: 0;
  gap: 8px;
  align: center;
}
.wrail-poster-tile__metadata {
  min-width: 0;
  color: var(--text-muted);
  font-size: 11px;
  line-height: 1.2;
  max-lines: 1;
  text-overflow: ellipsis;
  flex-grow: 1;
  flex-shrink: 1;
}
.wrail-poster-tile__state {
  color: var(--accent);
  font-size: 11px;
  font-weight: 600;
  line-height: 1.2;
  max-lines: 1;
  text-overflow: ellipsis;
  flex-grow: 0;
  flex-shrink: 0;
}

/* Diagnostics and commands remain presentational and controller-neutral.
   Consolas is the single stable monospace family in the supported Windows
   baseline; the native font field does not currently implement CSS stacks. */
.wrail-code-text {
  min-width: 0;
  padding: 8px 10px;
  background: var(--surface-subtle);
  color: var(--text);
  border-color: var(--border);
  border-width: 1px;
  corner-radius: 8px;
  font-family: Consolas;
  font-size: 13px;
  font-weight: 400;
  line-height: 1.4;
  max-lines: 8;
  text-overflow: clip;
  text-align: start;
}

.wrail-responsive-grid {
  min-width: 0;
  min-height: 0;
  gap: 8px 10px;
}

.wrail-window-preview {
  width: 100%; min-width: 0; flex-shrink: 0; overflow: clip;
  background: var(--surface); color: var(--text-muted);
  corner-radius: 6px; font-size: 12px; text-align: center;
}
.wrail-preview-poster { flex-wrap: nowrap; gap: 8px; min-width: 0; }
